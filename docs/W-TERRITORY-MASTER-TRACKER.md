# W-TERRITORY-MASTER tracker

**Status:** Tracker v30 (2026-06-01). Model LOCKED (v16, 2026-05-28). Phase 1 + Phase 2 + P-LIFECYCLE Landing 1 SHIPPED; P-LIFECYCLE Landing 2 SHIPPED (primitive `2ca82c6` + sync hooks `433c553`); P-LIFECYCLE Landing 3 Event 4 SHIPPED (`9255a18`, async via `territory_reroll_queue` + cron); F-apa-secdef-sweep SHIPPED (`d2f0e69`, all 3 `handle_apa_*` handlers DEFINER, F-EXISTING-HANDLE-APA-TRIGGERS-SAME-LATENT-RISK closed); P-DASHBOARD CORE-5 SHIPPED (`3a4167a`, 5 operator-cockpit gaps closed: discoverability redirect, agent footprint + reflow-preview, floor-pool alerts surface, cron observability, NULL-cache visibility; GAP-7 closed); F-NIGHTLY-SYNC-TIMEOUT-6H fixes 1+2+4 applied (`c77ce01`, awaiting first-night confirmation); **W-CORE-VERIFICATION complete (CV-RECON → CV-COMMIT `52c908e` + v5 reopen `4cf4eed`) — territory/hierarchy/leads/email/AI proven end-to-end on live WALLiam;** **P1 FIX 1/3 SHIPPED 2026-06-01 — F-ESTIMATOR-MIXES-RENT-INTO-SALE-COMPS closed (3 lines added to 2 sale-matcher files); F-ESTIMATOR-SUBLEASE-NOT-MATCHED filed as P3;** **P1 FIX 2/3 SHIPPED 2026-06-01 — F-REROLL-LISTINGS-AT-GEO-COUPLED-CHECK closed (trio-atomic `reroll_listings_at_geo` via NULL-then-delegate to `reresolve_listings_in_set`; SECURITY DEFINER + locked search_path; git-drift reconciled); smoke 5/5 PASS incl. pin survival + bit-identical walk-equivalence;** Landing 3 Event 7 (nightly reconcile) remains. This document is the authoritative spec for territory + lead routing.

Read `CLAUDE.md` (repo root) first for the engineering constraints that govern all work here.

---

# PART 1 — THE LOCKED MODEL

This is the canonical, long-term architecture. Do not deviate without an explicit decision recorded in this tracker.

## Core primitive: the routing set

One mechanism, three sizes:
- A **routing set** = 1..N agents attached to a `(scope, scope_id, property_type)` node.
- **Assignment** = N=1 (one agent owns the scope).
- **Distribution** = N>1 (agents share; each listing/sub-geo hashes to one).
- **Floor** = the routing set at the tenant root (catches everything uncarved).

Assignment, distribution, and floor are ONE feature at three sizes. Build the routing-set resolver once. "Distribute Durham among 20" = insert 20 routing-set rows at the Durham area node. There is never a fourth mechanism.

## Two orthogonal axes

1. **Geo specificity** (most-specific-first walk):
   `listing pin → building → neighbourhood → community → municipality → area → tenant floor`
2. **Property type**, applied at EVERY geo level (not just community):
   `condo | home`

A routing set targets a `(geo-level, property-type)` pair. "Durham-area condos → Agent A" and "Durham-area homes → Agent B" are two independent routing sets at the same geo level. The condo/home split exists at neighbourhood, community, municipality, AND area. Building level is condo-only (homes are orphans — no building parent — and skip it).

## Precedence: most-specific wins, enforced by STORED scope-level

Resolution walks most-specific → least-specific; first level with a routing set for the listing's property type wins; walk stops there.

**Precedence is a property of stored data, not execution order.** Each materialized cache row records WHICH scope-level set it (`assignment_scope` + `assignment_source_id`). A distribution at level L only overwrites cache rows whose current scope is L-or-broader — it NEVER clobbers a more-specific carve. Distributions are safely re-runnable in any order, any number of times. Re-running tenant-level distribution months later does NOT wipe area/community/building carves. This is the property that makes "never revisit" true.

## Materialized on-demand (NOT live resolver)

The resolved owner of every listing is **materialized** to a cache column. Display and leads read one indexed column — fast everywhere. Expensive resolution (geo walk + hash) happens at **distribution time** (operator-triggered, or scoped delta on data events), NOT on every page render or lead.

Rationale (verified by latency test, `scripts/r-p-floor-latency-test.js`): hash compute is cheap (0.39ms/geo pure DB) but a live-resolve-on-every-render model puts the resolver on 100% of the read path with per-call round-trip cost. Materializing removes the read-path resolver entirely. The "pure resolver, nothing stored" model was explicitly evaluated and DROPPED in favor of materialized-on-demand.

## Sticky distribution bindings

Distribution bindings (N>1) HOLD once materialized. Adding an agent to a set does NOT reshuffle existing bindings. Only three things re-materialize an existing row:
1. Agent departure/deactivation (reflow scoped to that agent's rows).
2. Explicit operator rebalance.
3. Carve removal (fall back to next-broader level).

Sticky is near-free under materialization — the binding is already stored; simply don't auto-rewrite on pool-add.

## Event lifecycle (every routing mutation + its scoped re-materialization)

| Event | Source | Re-materialization scope |
|---|---|---|
| Operator runs distribution | Operator | Rewrite that scope only; skips more-specific carves |
| Operator assigns (N=1) | Operator | Rewrite that scope, instant; overrides broader |
| Operator removes a carve | Operator | Fall back to next-broader level; re-resolve released scope |
| Agent departs / deactivated | System (flag) | Reflow that agent's rows only; leads+emails blocked instantly (resolver already filters `is_active AND is_selling`) |
| New listing arrives (nightly sync) | System (data) | Resolve at insert; delta only — cheap |
| Listing geography changes | System (sync diff) | Re-resolve that listing |
| Periodic reconciliation | System (scheduled) | Re-resolve mismatches; corrects stale cache (drift safety net) |

## Property-type asymmetry

- **Condos** (`'Residential Condo & Other'`): have building parent. Building set overrides community set. Walk: pin → building → community → muni → area → floor.
- **Homes** (`'Residential Freehold'`): orphans, no building. Walk: pin → community → muni → area → floor.
- Building-scope routing sets valid for condos only; invalid/ignored for homes.

## Lead alignment

- Display agent === lead agent — both read the materialized owner; cannot diverge.
- One agent per geo (not per-listing-card chaos). Buyer sees one consistent face per geography.
- Lead routed at arrival → stamped to `leads.agent_id` → frozen. Pool changes never disturb an already-routed lead.
- Lead flows up the stamped agent's hierarchy chain (`manager_id`, `area_manager_id`, `tenant_admin_id`) — existing W-HIERARCHY + R7 delegation BCC machinery.

## Open design decisions — NOW DECIDED (2026-05-28), do not re-ask

- **A. New-listing routing at insert — DECIDED: resolve-at-insert.**
  When the nightly PropTx sync inserts a new listing, resolve that one listing through the materialized path and write its `assigned_agent_id` in the same operation. Delta only (hundreds/night) — cheap. Honors "no time anything is unclaimed." Reuses the exact resolution the distribution engine uses (one code path). If resolution finds no routing set (shouldn't happen when a tenant floor exists, but defensively), fall to the tenant floor. New listings are NEVER left NULL.

- **B. Cache invalidation / reconciliation — DECIDED: triggers (primary) + incremental nightly reconcile (safety net).**
  - **Mutation-triggered re-resolution** is the primary mechanism: the 7 lifecycle events each fire scoped re-materialization the moment they happen. Keeps the cache correct in real time.
  - **Periodic reconcile job** is the safety net for missed triggers / manual edits / sync-without-hook. Runs nightly after sync. **Must be INCREMENTAL and BOUNDED** — only checks rows that could have changed (sync-delta that night + flagged rows + a small rolling sample), NOT a full 1.28M re-resolve. Full-table re-resolve is reserved for an explicit operator-triggered "rebuild" button only. The reconcile job doubles as drift telemetry: if it corrects many rows nightly, a trigger is missing.

- **C. Sticky scope — DECIDED (confirmed earlier):** distribution bindings hold; only departure-reflow, explicit rebalance, or carve-removal re-materialize.

---

# EXECUTION PROTOCOL — follow this rhythm for every phase

This project is executed with strict recon-first, baseline-before-change, smoke-after discipline. Nothing is guessed; nothing production-touching runs without review. Follow this protocol on EVERY phase, not just the risky ones.

## The rhythm (per phase)

1. **RECON FIRST (read-only).** Before writing any build code for a phase, run a read-only probe that verifies every assumption the phase depends on — schema shape, existing function bodies, current data state, constraint definitions. Output to a `*-output.txt`. One script, one run. Use explicit column allow-lists (never `SELECT *` on credential-bearing tables). Show the operator the output. Do not write build code until the recon output is reviewed.

2. **PROPOSE + WAIT.** State the build plan in plain language: what files change, what the migration does, what could regress. For any phase that WRITES to the DB or commits, show the full migration SQL and the apply-runner, then STOP and wait for explicit operator approval. Do not apply on your own initiative.

3. **BASELINE (for any phase that changes routing/resolution).** Before applying, run a smoke script against CURRENT production that captures the truth that must be preserved — specifically: the 12 Whitby carves (Neo Smith @ Whitby muni; King Shah @ the 11 named communities), a sample of floor-routed listings, the homes (NULL or otherwise), and cross-tenant (aily) isolation. Save this baseline to a file. This is the "what must not change" snapshot.

4. **APPLY (transactional, reversible).** Production DB changes use a `node scripts/apply-*.js` runner that: (a) captures a rollback snapshot of every function it will replace, to `supabase/migrations/rollback-snapshots/`; (b) runs the migration inside ONE transaction; (c) runs post-state verification checks; (d) COMMITs only if ALL checks pass, else ROLLBACK. Never apply outside this pattern.

5. **SMOKE AFTER + DIFF.** Re-run the same smoke script post-apply. DIFF against the baseline from step 3. Any listing that changed owner unexpectedly is a regression — HALT and show the operator. Only intended changes (e.g. the ~1.29M reverted-then-re-materialized rows: 495,428 condos + 799,688 homes) may differ; everything else (the 12 carves, aily) must be byte-identical.

6. **COMMIT (reviewed).** Show what is being committed. `git add` + commit with a descriptive message after the operator confirms.

7. **UPDATE THIS TRACKER (mandatory, non-skippable).** Before the phase is considered closed, update this tracker on disk:
   - Move the phase's row in the roadmap to CLOSED with the commit SHA.
   - Add a dated entry to PART 4 (Condensed History) stating: what was applied, what production state changed (with verified numbers, not estimates), what was committed, and any new finding or deviation discovered.
   - If anything was learned that future sessions must know (a schema surprise, a constraint, a gotcha), add it to PART 6 (Process Lessons).
   - If a new open item surfaced, add it to PART 5 (Open Findings).
   - **Back up the tracker before editing it** (it is an existing file): `Copy-Item docs/W-TERRITORY-MASTER-TRACKER.md docs/W-TERRITORY-MASTER-TRACKER.md.backup_<timestamp>` BEFORE the edit.
   - Commit the tracker update in the same commit as the phase work (or immediately after).

   **Why this is non-skippable:** Claude Code sessions do not persist. The next session — possibly days later, possibly a cold start that remembers nothing — resumes ONLY from this tracker and the git history. An un-updated tracker means the next session starts blind and may assume a clean slate that does not match production (the exact failure this whole protocol guards against). The tracker is the memory between sessions. Treat updating it as part of the work, not paperwork after it.

## At the START of every session (cold-start checklist)

Before proposing or doing anything, a fresh session MUST:
1. Read `CLAUDE.md` and this tracker in full.
2. Run `git status` and `git log --oneline -10` to see what is committed vs uncommitted.
3. Run a read-only DB state check against the verified-state facts in PART 2 (e.g. count NULL vs filled `assigned_agent_id`, count `tenant_floor_pool` rows, count apa rows) and CONFIRM the live DB matches what the tracker says the last session left. If the DB and tracker disagree, STOP and surface it to the operator — do not build on a mismatched assumption.
4. Only then propose the next action.

## Phases that run AUTONOMOUSLY (no per-step approval needed)

- All read-only recon probes.
- All smoke tests (they read, they don't write).
- Local `npm run dev` smoke checks.

## Phases that REQUIRE operator review before executing

- Any migration / DDL / DML against the production DB.
- The ~1.29M-row backfill revert (495,428 condos + 799,688 homes — destructive — see safety note below).
- Any `git commit` / `git push`.
- Any change to `resolve_agent_for_context` (13 direct/indirect callers + 1 placeholder — see frozen-contract note).

---

# REGRESSION + HARM GUARDS — mandatory

## Frozen contracts (never change without explicit operator sign-off)

- **`resolve_agent_for_context` SIGNATURE is frozen.** It has 13 direct/indirect callers + 1 placeholder (SimulatorTab stub) — corrected from the original v18 count of 9 after Phase 2 recon. 7 are cache-first eligible (have `listing_id` in scope, wired in Phase 2 EDITs A/B/C/D): `lib/utils/tenant-resolver.ts` (the wrapper invoked by 5 geo-page renders), `lib/actions/leads.ts` (leads helper), and 5 API routes (charlie/lead, walliam/contact, walliam/charlie/session, walliam/estimator/session, walliam/assign-user-agent). The remaining 6 are RPC-only by design (no `listing_id` in scope): 5 geo-page render call-sites (MunicipalityPage, CommunityPage, AreaPage, BuildingPage, toronto/[neighbourhood]) + charlie/appointment. Change the BODY only — never the parameters, never the return type. A signature change silently breaks Charlie, leads, contact, estimator.
- **The 12 existing apa carves must survive every migration.** Post-migration, Whitby-community condos still resolve to King Shah; Whitby buildings still resolve to Neo Smith. The smoke matrix asserts this explicitly.
- **System 1 isolation is absolute.** Territory work is all System 2. If any proposal touches `/admin`, `app/api/chat/*`, or `agent_buildings` — STOP, that is a red flag, surface it to the operator.

## The backfill revert — the one destructive operation

Setting ~1.29M rows (495,428 condos + 799,688 homes) `assigned_agent_id` to NULL is destructive IF done alone. It is safe ONLY because it happens in the SAME TRANSACTION as the v16 geo-level re-materialization that immediately re-fills them. **Structure it as: revert + re-materialize commit together or roll back together — never two separate transactions.** A split would leave ~1.29M listings unowned in a visible window. The apply-runner must verify post-state has zero unintended NULLs before COMMIT.

## Baseline-diff is the primary regression detector

For the migration phase: capture the resolution truth BEFORE (baseline), apply, capture AFTER, diff. Anything that moved that shouldn't have is caught automatically. This is stronger than eyeballing — it is a mechanical assertion that only intended rows changed.

## No mass operation without a timeout + compute plan

1.28M-row operations need `SET statement_timeout = 0` (gated by env var) and adequate Supabase compute. Prefer the materialized model that avoids mass rewrites; where a large operation is unavoidable (initial materialization, explicit rebuild), batch it or run on Medium compute and downgrade after.

## Local smoke before any prod-facing push

Nothing reaches a prod-facing surface until local `npm run dev` smoke passes. Never test on Vercel preview.

## GAP guards — additional mandatory checks (added 2026-05-28)

These close gaps identified in execution review. Apply at the relevant phase.

- **GAP 2 — Smoke-diff must assert NEW VALUES, not just detect change.** For the ~1.29M reverted-then-re-materialized rows (495,428 condos + 799,688 homes), the diff must confirm each one's new `assigned_agent_id` equals what the v16 geo-level resolver would produce — not merely that it changed. "Changed" is insufficient; a wrong-but-changed value would pass a change-only diff. Assert the new value matches expected resolver output for that listing's geo + property type. Everything OUTSIDE the reverted set must be byte-identical to baseline.

- **GAP 3 — Write the DOWN migration before applying the UP.** Before the P-ROUTING-SET + P-MATERIALIZE + P-REVERT-BACKFILL migration is applied, author and show the operator the explicit rollback: how to undo this specific change if a regression surfaces AFTER commit (when the transaction-level rollback no longer applies). Verification checks only catch what they test; the down-migration is the recovery for what they miss. New tables/columns: how to drop. Reverted+re-materialized rows: how to restore prior state (or confirm prior state was the known-wrong listing-level backfill and document that restoring it is not desired). Must cover BOTH condos AND homes — the ~1.29M reverted rows include 495,428 condos + 799,688 homes (homes confirmed 2026-05-28 as same v15 listing-level picks). No up-migration applies without a written, reviewed down-migration.

- **GAP 4 — Reconcile job action-on-mismatch is DEFINED: correct + log + threshold-alert.** When the nightly incremental reconcile finds a cache row whose owner != current resolver output: (a) correct it (that is reconcile's job), (b) log every correction to a `reconcile_corrections` table (listing_id, old_agent, new_agent, reason, timestamp), (c) if corrections in one run exceed a threshold (start at 50), alert the operator — because high correction volume means a mutation trigger is missing and silently drifting. Reconcile must never silently mass-correct; visibility is mandatory so a broken trigger surfaces instead of being masked.

- **GAP 5 — Departure boundary: in-flight leads complete, only NEW leads blocked.** Deactivating an agent blocks routing of leads arriving AFTER the deactivation commits. A lead already stamped and mid-processing (email/BCC in flight) completes normally — it is already owned. Do not attempt to claw back in-flight leads. Stated decision, not accidental timing.

- **GAP 6 — Materialized read path must be scale-tested.** Before declaring the build done, time the realistic production read: a geo page / search grid reading `assigned_agent_id` for many listings joined to agent profiles, under realistic row counts. Almost certainly fast (indexed column), but the no-guessing rule applies — 100% of production traffic hits this path. One timing query; record the number in the tracker.

---

# PART 2 — RECONCILIATION OF PRIOR WORK (v15 → v16)

A narrow version of this model shipped on 2026-05-27 (tenant-root floor only, listing-level hash, cached via backfill). Under the v16 model:

- **KEEP:** `tenant_floor_pool` table, `tenant_floor_alerts`, the resolver floor branch, audit/queue triggers (`handle_tenant_floor_pool_change`, `trg_tfp_after_*`), the `territory_assignment_changes.change_type` CHECK extension (+floor_pool_added/removed/access_changed), the `territory_reroll_queue.scope` CHECK extension (+tenant_default), `uq_trq_pending_dedup`. These constitute the tenant-root routing set — valid as the broadest distribution.
- **SUPERSEDE:** the listing-level hash in `pick_floor_agent`. v16 is geo-level + sticky, not per-listing. `pick_floor_agent` needs rework to hash at geo scope and respect stickiness.
- **REVERT:** the ~1.29M-row v15 listing-level backfill — 495,428 condos + 799,688 homes (homes confirmed 2026-05-28 as same v15 `reroll_listings_at_floor` picks; v15's "connection died mid-run" was a client disconnect after the DB-side transaction had already committed — see F-HOMES-FILLED-UNEXPECTEDLY and v17 history entry). Under v16 the floor is geo-level and materialized via the unified distribution path. Action: set those ~1.29M `assigned_agent_id` back to NULL, re-materialize via v16 geo-level distribution. Both condos and homes are reverted together in the same transaction.
- **GENERALIZE:** `tenant_floor_pool` is the root-scope special case of a general routing-set spanning all scopes × property type. `agent_property_access` already has scope + condo/homes/buildings flags + is_primary. RECON NEEDED: how much of the general routing-set primitive does `agent_property_access` + `distribute_listings_at_geo` already provide vs. net-new.

### Verified production state as of v16 (re-verify before building)

- `mls_listings`: ~1,295,935 rows. As of v15 backfill: condos materialized (listing-level, to be reverted), homes NULL, 12,547 pre-existing cascade assignments untouched. [RESOLVED 2026-05-28 — homes are NOT NULL: 799,688 of 801,325 homes have `assigned_agent_id` filled. Source identified: v15 `reroll_listings_at_floor` homes run DB-committed despite client disconnect — same listing-level `pick_floor_agent` picks as condos, requiring the same revert. See F-HOMES-FILLED-UNEXPECTEDLY.]
- `agent_property_access`: 12 WALLiam rows — 1 Neo Smith @ Whitby muni (all flags incl. buildings=true), 11 King Shah @ Whitby communities (condo=true homes=true buildings=false). All `is_primary=true is_active=true`.
- `tenant_floor_pool`: 3 WALLiam rows (King Shah, Neo Smith, WALLiam seed agent), all condo+homes access.
- Resolver functions, full bodies, captured in `scripts/r-p-floor-recon.js` output (v13).
- `resolve_agent_for_context` has 13 direct/indirect callers + 1 placeholder — signature is a stable contract, change body only (see frozen-contract note for the cache-first/RPC-only breakdown). Original v18 count of "9" was incomplete; corrected during Phase 2 recon.

---

# PART 3 — PHASE ROADMAP

| Phase | Name | Status |
|---|---|---|
| P1–P5.3 | All prior territory phases | CLOSED |
| P-FLOOR-recon | 3-round read-only probe of resolver + apa + agents | CLOSED |
| P-FLOOR-migration | Narrow floor schema + resolver | APPLIED, superseded by Phase 1 |
| P-MODEL-LOCK | Comprehensive long-term model | LOCKED (this doc) |
| P-MODEL-recon | Probe routing-set coverage in `agent_property_access` + `distribute_listings_at_geo` + cache | CLOSED (output: `r-p-model-recon-output.txt`) |
| P-ROUTING-SET | Build/generalize the unified routing-set primitive (all scopes × property type) | CLOSED (Phase 1, v19) |
| P-MATERIALIZE | On-demand distribution engine + scoped re-materialization per event | CLOSED (Phase 1, v19) |
| P-REVERT-BACKFILL | Revert ~1.29M rows (495,428 condos + 799,688 homes) listing-level cache to NULL | CLOSED (Phase 1, v19) |
| Phase 2 | Cache-first reader wiring across 7 callers (wrapper + leads helper + 5 API routes) + tenant-scope fix | SHIPPED 2026-05-29 (EDIT A `7b5a59c` → EDIT B `4170ba6` → EDITs C.1-C.4 `cfc3710` → EDIT C.5 `da86f13` → EDIT D `748ddd8`; v20 tracker `e9d1662`) |
| **P-LIFECYCLE** | **Wire the 7 lifecycle events (insert-resolve, departure-reflow, geo-change, reconcile, etc.)** | **IN PROGRESS** — Landing 1 SHIPPED 2026-05-29 (`4e69773`, v21 tracker `d1d7d4f`); Landing 2 primitive SHIPPED 2026-05-29 (`2ca82c6`); Landing 2 sync hooks SHIPPED 2026-05-30 (`433c553`, Events 5+6 wired into all 6 ingestion surfaces); Landing 3 Event 4 SHIPPED 2026-05-30 (`9255a18`, async via `territory_reroll_queue` + cron); Landing 3 Event 7 (nightly reconcile) REMAINS |
| P-DASHBOARD | Operator UI: run distributions, view coverage, assignment-vs-default per geo | OPEN (blocked on GAP-7 design pass) |
| P-SMOKE | Full matrix: precedence, property-type split, sticky, departure-reflow, cross-tenant | **SHIPPED 2026-06-01** (`52c908e` + v5 reopen `4cf4eed`; see `docs/W-CORE-VERIFICATION-TRACKER.md` — 198/198 build-phase smoke across CV-TERRITORY/HIERARCHY/LEADS/CROSS, plus v5 real-key end-to-end on live WALLiam: real Resend msg ids, real $561K Whitby valuation, real Sonnet 4 AI commentary) |
| P-COMMIT | Atomic ship | OPEN |

## P-MODEL-recon — specific questions to answer (read-only, no writes)

1. Does `agent_property_access` already support multiple non-primary rows at one geo (i.e. a distribution set), or only one primary per geo? Check the constraints and existing data.
2. Does `distribute_listings_at_geo` already hash listings across multiple apa agents at a geo? Read its body (captured in `scripts/r-p-floor-recon.js` output) and confirm.
3. Is there any column on `mls_listings` (or a side table) recording WHICH scope-level set the current `assigned_agent_id` — i.e. the precedence provenance the v16 model requires? If not, it's net-new.
4. How do `reresolve_listing` / `reresolve_building` propagate to the cache, and do they respect property type?
5. What does the nightly sync (GitHub Actions) currently do with `assigned_agent_id` on new listings — set it, leave NULL, or nothing?
6. Map all readers of `mls_listings.assigned_agent_id` (display path, lead path) — confirm they read the cache, and that display and lead paths agree.

Output to a single `r-p-model-recon-output.txt`. One script, one run. Use explicit column allow-lists (never `SELECT *` on `tenants`/`agents`).

## Suggested build order (each phase follows the EXECUTION PROTOCOL above)

1. **P-MODEL-recon** — one read-only script answering the 6 questions. Review output before building. (Autonomous to run; review output.)
2. **P-ROUTING-SET + P-MATERIALIZE + P-REVERT-BACKFILL** — ONE atomic migration: generalize the routing-set primitive (extend `agent_property_access` semantics and/or add scope-level provenance columns to the cache), build the on-demand distribution engine that materializes with scope-level provenance, AND revert the ~1.29M rows (495,428 condos + 799,688 homes) + re-materialize them via the v16 geo-level path — all in one transaction. Apply-runner with snapshot + verification + transactional commit. REQUIRES baseline-capture before, smoke-diff after, operator review of the SQL before apply.
3. **P-LIFECYCLE** — wire the 7 events: resolve-at-insert hook in the nightly sync (decision A), mutation triggers for the operator/agent events, incremental nightly reconcile job (decision B). REQUIRES operator review before apply.
4. **P-DASHBOARD** — operator UI: run distributions, view coverage, assignment-vs-default per geo, see floor alerts. REQUIRES review before apply.
5. **P-SMOKE** — full matrix locally (`npm run dev`) before commit: precedence (pin > building > community > muni > area > floor), property-type split at every level, sticky (add agent → existing bindings unchanged), departure-reflow (deactivate agent → their rows re-flow, leads/emails blocked), cross-tenant isolation (aily never gets WALLiam agents). Autonomous to run; review results.
6. **P-COMMIT** — atomic. Review what's committed.

A and B are DECIDED (see above) — do not pause to re-ask. C is confirmed. Pause for operator review at the write-phases per the EXECUTION PROTOCOL, not for already-decided design questions.

### Phase 1 migration package — FINALIZED + COMMITTED (2026-05-29, attempt 4)

Three files on disk in final reviewed form (NOT executed, NOT committed):
- `supabase/migrations/20260528_phase1_routing_set_and_revert.sql` (561 lines) — up-migration
- `supabase/migrations/20260528_phase1_down.sql` (129 lines) — down-migration (GAP-3)
- `scripts/apply-phase1-routing-set.js` (427 lines) — apply-runner

Locked Phase 1 / Phase 2 split: Phase 1 corrects the cache in one atomic transaction; Phase 2 (separate, after Phase 1 smoke confirms cache is correct) wires the 9 `resolve_agent_for_context` callers to read `assigned_agent_id` first. Readers must NOT trust the cache until Phase 1 has corrected it — wiring before correction would route real leads to wrong agents silently.

Locked design decisions:
1. `assigned_source_id` for floor picks: set to `tenant_floor_pool.id` (load-bearing for precedence-by-stored-scope).
2. aily fixture: REMOVED from migration (no synthetic agent inserts into credentials-bearing tables); V6 stays vacuous-but-correct; real isolation smoke deferred (F-AILY-CROSS-TENANT-SMOKE-DEFERRED).
3. Transaction: `DISABLE_STATEMENT_TIMEOUT=1`, Medium compute, downgrade to Micro as non-skippable closing step.
4. `tenant_floor_alerts` writes inside same transaction.
5. Revert scope ~1.29M (495,428 condos + 799,688 homes) — confirmed v15 backfill picks (F-HOMES-FILLED-UNEXPECTEDLY).
6. Per-cache-change audit table: WAIT for reconcile work (GAP-4); provenance covers per-row "what set this."

Pre-execution requirements:
- `DATABASE_URL` = direct connection (`db.<project>.supabase.co:5432`), NOT pooler
- `DISABLE_STATEMENT_TIMEOUT=1` in `.env.local`
- Supabase compute upgraded to Medium before run
- Closing step (manual, non-skippable): downgrade compute Medium → Micro in dashboard after successful COMMIT

Verification coverage (V1–V8 + V7d):
- V1: provenance columns + CHECK constraints exist
- V2: slot-constraint swap (`uq_apa_active_slot` → `uq_apa_active_slot_per_agent`)
- V3: coupled-state invariant
- V7d (diagnostic, runs BEFORE V4a): WALLiam floor pool has effective condo + homes coverage
- V4a (PRIMARY): zero condo/home NULLs after re-materialize
- V4b sanity: total filled in 1,297,000–1,299,000
- V4c: coupled-state re-check
- V5a: Whitby-muni-outside-carves → Neo Smith @ municipality (NOT EXISTS, disjoint from V5b)
- V5b: King Shah's 11 communities @ scope=community (COUNT DISTINCT = 11)
- V5c: 2 Commercial pins preserved
- V6: cross-tenant isolation (vacuous against aily today)
- V7a: floor > 1,200,000 / V7b community > 0 / V7c municipality > 0
- V8: zero `empty_floor_pool` alerts in the window

Failure/recovery:
- ASSERT failure inside transaction → automatic ROLLBACK, no DB state changed, snapshots remain on disk
- Post-COMMIT regression → run down-migration: STEP 1 `psql -f` both rollback-snapshot files, STEP 2 `psql -f 20260528_phase1_down.sql`. Default leaves cache at v16-correct values; D.6 hard-restore optional and not run by default.

Attempt 4 outcome (2026-05-29, COMMITted):
- Three prior attempts rolled back cleanly before reaching COMMIT. Attempt 1: V2 coupled CHECK validated against 1.29M existing rows whose newly-added `assigned_scope` column was NULL → fixed via `ADD CONSTRAINT … NOT VALID` in §2 + `VALIDATE CONSTRAINT` in new §7.5 after re-materialization. Attempt 2: V5a `ASSERT v_n > 0` failed because King Shah's 11 community carves are exhaustive of Whitby muni (target set empty) → fixed via vacuous-pass Option C with refinement (positive boundary on Neo Smith + negative boundary on King Shah). Attempt 3: direct-host DNS (`db.<projref>.supabase.co`) unresolvable for this Supabase project — newer projects are pooler-only → fixed by switching `DATABASE_URL` back to session pooler (port 5432) + relaxing runner's pooler-heuristic to port 6543 only (transaction pooler) since session pooler preserves `SET LOCAL statement_timeout`.
- Attempt 4 transaction: **89 min wall time** (5,370.9s) for the 1.29M-row revert + re-materialize on Medium compute.
- **All V-asserts passed inside the transaction** (V1, V2, V3, V7d, V4a, V4b, V4c, V5a, V5b, V5c, V6, V7a, V7b, V7c, V8, plus §7.5 `VALIDATE CONSTRAINT`).
- **Post-COMMIT cache verified correct** via read-only probe (`scripts/investigate-phase1-post-commit.js`):
  - 12,621 Whitby community condo/home listings route to King Shah @ scope='community' (all 11 carves: Blue Grass Meadows 1217, Brooklin 1543, Downtown Whitby 1210, Lynde Creek 834, Port Whitby 1255, Pringle Creek 1591, Rolling Acres 1186, Rural Whitby 1870, Taunton North 771, Whitby Industrial 13, Williamsburg 1131 — zero mismatches).
  - 2 Commercial pins preserved: Neo Smith @ scope='municipality' with `assigned_source_id` set to the Neo Smith Whitby-muni apa row.
  - 1,284,892 condo/home listings at scope='floor' across 3 WALLiam pool agents — 3-way hash split: 428,444 / 428,259 / 428,189 (delta < 0.06%).
  - V3 coupled-state invariant holds (zero violations).
- **63 post-COMMIT smoke regressions identified as false positives** — all 63 are in carve samples. 58 "agent changed" reflect v15 floor-pool picks correctly being replaced by v16 community carves (expected); 5 "wrong scope" are Whitby muni baseline samples that the runner's baseline-capture incorrectly expected at scope='municipality' (they're inside community carves, community precedence wins). Cache is correct; runner's baseline-capture has a latent bug to fix in a future runner revision.
- **34 NULL condo/home rows accumulated in 1h38m post-COMMIT** — this is the resolve-at-insert gap (Decision A) confirmed as the live drift source. Tracked as F-RESOLVE-AT-INSERT-PRIORITY (PART 5).
- GAP-6 read-path timing: 1.88s for 17 rows in one Whitby community — slow; worth investigating separately when Phase 2 starts wiring readers.
- Migration `.sql` files and apply-runner remain untracked in git (no git commit of the artifacts themselves; the DB COMMIT is the source of truth).

### Phase 2 — CLOSED 2026-05-29 (cache-first reader wiring + tenant-scope fix)

Phase 2 wires the 7 cache-first eligible callers of `resolve_agent_for_context` to read `mls_listings.assigned_agent_id` before falling through to the RPC. Cache-first reads JOIN against `agents.tenant_id` filtered by the caller's tenant_id — cross-tenant cached agents are treated as cache-miss and fall through to the RPC's already-tenant-scoped path. **Frozen contract preserved**: RPC body + signature untouched across all 5 commits.

Commit chain (on `main`, not pushed):
- EDIT A `7b5a59c` — `lib/utils/tenant-resolver.ts` (wrapper used by 5 geo-page renders)
- EDIT B `4170ba6` — `lib/actions/leads.ts` (leads helper)
- EDITs C.1–C.4 `cfc3710` — charlie/lead, walliam/contact, walliam/estimator/session, walliam/charlie/session (4 API routes, single batched commit)
- EDIT C.5 `da86f13` — walliam/assign-user-agent (cache-first + provenance-based source-label switch on `assigned_scope`; uses snake_case `auto_floor` per operator clarification)
- EDIT D `748ddd8` — tenant-scope fix across all 7 files (closes F-PHASE2-CACHE-NOT-TENANT-SCOPED)

Smoke outcomes (E2E + EDIT D fix-proof):
- CARVED (WALLiam POST on Brooklin condo `f8a24890`): PASS — King Shah returned. Cache-first short-circuits RPC, verified via dev log: `[session] route hit` without `[session] calling rpc`.
- FLOOR (WALLiam POST on non-Whitby condo): PASS — Neo Smith returned (exact pre-named UUID from cache, not "a floor agent").
- **CROSS-TENANT (aily POST on WALLiam-cached `f8a24890`): FAILED on E2E → empirical fix-proof for F-PHASE2-CACHE-NOT-TENANT-SCOPED in EDIT D smoke.** Pre-EDIT-D `lead.tenant_id = aily, lead.agent_id = King Shah` (WALLiam agent — the leak). Post-EDIT-D `lead.tenant_id = aily, lead.agent_id = NULL` (cache-first INNER JOIN filtered cross-tenant agent → fell through to RPC → RPC tenant-filter returned null → graceful admin-routed lead).
- NULL-CACHE (WALLiam POST on `assigned_agent_id IS NULL` listing): PASS-with-caveat — `walliam/contact` returned graceful null-agent lead; same listing on `assign-user-agent` path hits F-FLOOR-POOL-PERMISSION-DENIED (pre-existing bug, bounded to NULL-cache, NOT a Phase 2 regression).

Smoke artifacts purged 2026-05-29: 13 leads (9 `phase2-smoke` + 4 `phase2-edit-d-smoke`) deleted in one transaction with strict-13 affected-row assertion (pre-tx count 13 → in-tx delete 13 → in-tx verify 0 → COMMIT → post-tx live verify 0).

### P-LIFECYCLE — IN PROGRESS (Landing 1 + Landing 2 SHIPPED 2026-05-29/30)

P-LIFECYCLE wires the 7 lifecycle events from PART 1's Event Lifecycle table. Recon (`phase-lifecycle-recon-output.txt`) found Events 1-3 (operator distribute/assign/remove-carve) already covered by Phase 1's `agent_property_access` AFTER triggers + admin-homes API routes; Events 4-7 + the F-FLOOR-POOL-PERMISSION-DENIED grant-layer fix are the active scope. Three independently-shippable landings, recommended order 1 → 2 → 3:

- **Landing 1 — F-FLOOR-POOL-PERMISSION-DENIED fix: CLOSED 2026-05-29, commit `4e69773`.**
  - `ALTER FUNCTION pick_floor_agent SECURITY DEFINER SET search_path = public, pg_temp`.
  - Root cause: `tenant_floor_pool` had grants ONLY for postgres + an RLS policy keyed on `auth.uid()` which evaluates to empty for server-side service-role callers. The resolver chain (`resolve_agent_for_context` -> `pick_floor_agent` -> SELECT `tenant_floor_pool`) ran as INVOKER and hit "permission denied for table tenant_floor_pool" at the table-grant layer before RLS even applied.
  - SECURITY DEFINER moves the floor SELECT to postgres privileges for the function's body only; INVOKER chain elsewhere preserved. Safe because Phase 2 EDIT D validated every cache-miss caller passes `p_tenant_id` from a validated request context (preconditions documented in `phase-lifecycle-landing-1-precondition.txt`, both PASS).
  - Empirical fix-proof inside the migration transaction:
    - V4: `SET LOCAL ROLE service_role; SELECT pick_floor_agent(...)` -> Neo Smith `f2ce3011-...`. Pre-ALTER the same call raised the permission-denied error.
    - V5: same probe escalated through the full `resolve_agent_for_context` chain -> returns non-null. End-to-end resolver path unblocked under service_role.
  - 5 V-asserts (V1 prosecdef flipped, V2 search_path locked, V3 postgres sanity, V4 service_role probe, V5 end-to-end) all PASS inside the same transaction; COMMIT followed.
  - Rollback-snapshot of pre-ALTER `pick_floor_agent` definition captured: `supabase/migrations/rollback-snapshots/_phase-lifecycle-landing-1_pick_floor_agent_2026-05-29T16-35-25-864Z.sql`.
  - Down-migration on disk (`20260529_phase_lifecycle_landing_1_down.sql`): `ALTER FUNCTION ... SECURITY INVOKER RESET search_path` + restored comment.

- **Landing 2 — Event 5 + Event 6: SHIPPED 2026-05-29/30. Primitive commit `2ca82c6` + sync hooks commit `433c553`.**
  - New PG function `public.reresolve_listings_in_set(p_listing_ids uuid[], p_tenant_id uuid) RETURNS TABLE(resolved_count int, null_count int)`. SECURITY DEFINER, `search_path = public, pg_temp`. Owner: postgres.
  - 10-level set-based cascade mirroring `resolve_agent_for_context` P-walk, narrowed by `ml.id = ANY(p_listing_ids)` at every level. Each UPDATE writes the provenance trio `(assigned_agent_id, assigned_scope, assigned_source_id)` atomically. Hash-RR via `PARTITION BY apa.<geo>_id` for multi-bucket batches; floor branch mirrors `reroll_listings_at_floor`.
  - **Tenant isolation 10/10:** every cascade level scopes BOTH the anchor predicate (apa.tenant_id / tfp.tenant_id / agent_listing_assignments-via-agents / agent_geo_buildings-via-agents) AND `agents.tenant_id = p_tenant_id`. Belt-and-suspenders against misconfigured apa/tfp rows pointing to another tenant's agent. Pre-fix Landing 2 had only anchor coverage on L3-L10; pre-apply audit caught the gap and added 10 `a.tenant_id` predicates before COMMIT.
  - **NULL-tenant predicate no-op (not an early guard):** the early guard validates only `p_listing_ids` shape; NULL tenant is filtered by the cascade itself (every `tenant_id = NULL` evaluates to unknown -> zero rows). V5 + smoke T4 prove this. Removes the guard-masks-real-bug class.
  - **Walk-equivalence modulo N=1 carves:** function uses hash-RR at carve levels (distribute pattern); `resolve_agent_for_context` P4/P5/P6 use primary-pick via `pick_routing_agent_for_type`. Agree at N=1; diverge by design at N>1 (v16's distribution-vs-assignment split). V2 picks an N=1 tenant for the walk-equivalence assert.
  - **`reresolve_listing` patched** to delegate to `reresolve_listings_in_set` with a one-element array. Closes F-RERESOLVE-COUPLED-CHECK: old body UPDATEd `assigned_agent_id` only and crashed on NULL-cache rows against the Phase 1 coupled CHECK.
  - **In-tx asserts V1-V6 all PASS in the apply transaction** (runtime-SELECTed listing/tenant ids, no UUID literals): V1 security shape; V2 NULL-cache walk-equivalence; V3 carved sticky guard; V4 empty array `(0,0)`; V5 NULL tenant predicate no-op; V6 patched `reresolve_listing` coupled trio.
  - **Smoke harness `scripts/smoke-phase-lifecycle-landing-2.js` T1-T5 all PASS:** T1 NULL-cache walk-equivalence + coupled trio; T2 carved sticky preserves pre-state; T3 empty array; T4 NULL tenant predicate no-op; T5 service_role end-to-end SECURITY DEFINER trust chain.
  - **Apply-runner post-COMMIT V3 hoist fix:** initial runner queried `tenant_floor_pool` directly under `SET LOCAL ROLE service_role` (service_role lacks the grant); fix hoists the tenant + listing picks out of the role-switched block, then switches role and calls the function with pre-picked values. Standalone re-verify PASSED. Same hoist pattern smoke T5 uses.
  - **Down-migration is DROP-only** (`20260530_phase_lifecycle_landing_2_down.sql`); the new down-runner (`scripts/apply-phase-lifecycle-landing-2-down.js`) reads the original `reresolve_listing` body from the captured rollback snapshot file and CREATE OR REPLACEs it on revert. No hardcoded function body in the down path.
  - **Rollback snapshot:** `supabase/migrations/rollback-snapshots/_phase-lifecycle-landing-2_reresolve_listing_2026-05-29T20-17-46-651Z.sql` (pre-Landing-2 body).
  - **Frozen contract preserved:** `resolve_agent_for_context` signature + body untouched.
  - **Sync hooks SHIPPED 2026-05-30, commit `433c553`** — see "Landing 2 sync hooks" sub-block immediately below for the full record.

#### Landing 2 sync hooks — SHIPPED 2026-05-30 (`433c553`)

Wired `reresolve_listings_in_set` into every production listing-ingestion path so Events 5 (resolve-at-insert) and 6 (geo-change re-resolve) fire automatically without depending on a reconcile job that is not yet built.

**Coverage correction at audit time (4 -> 6 surfaces):** the planning recon initially listed 4 ingestion surfaces. The pre-write duplication audit found `backfillListingGeoIds` exists in TWO additional production paths (`scripts/bulk-discover-assign.ts` CLI bulk discovery, `app/api/admin/bulk-discovery/db-assign/route.ts` admin discovery route) that also UPDATE `mls_listings.{area_id, municipality_id, community_id}` after `link_listings_to_building`. Both were added to the wire-set before any write. **All 6 surfaces wired:**

1. `lib/homes-sync/save.ts` (Next.js admin homes sync) -- post-upsert reresolve in `saveListings`
2. `scripts/lib/homes-save.ts` (cron homes sync, near-duplicate of #1 -- see F-SYNC-LAYER-DUPLICATION) -- post-upsert reresolve
3. `lib/building-sync/save.ts` -- post-upsert reresolve in `saveListings` + `backfillListingGeoIds` widened to `Promise<string[]>` with `.select('id')` + internal Event 6 reresolve
4. `scripts/sync-buildings-incremental.ts` -- inline `backfillListingGeoIds` same rework
5. `scripts/bulk-discover-assign.ts` -- inline `backfillListingGeoIds` same rework
6. `app/api/admin/bulk-discovery/db-assign/route.ts` -- inline `backfillListingGeoIds` same rework

**New files (3):**
- `lib/utils/territory-constants.ts` -- `WALLIAM_TENANT_ID` call-site constant.
- `lib/utils/geo-diff.ts` -- `readPreviousGeo` (explicit column allow-list: `id, listing_key, area_id, municipality_id, community_id, building_id`; neighbourhood_id intentionally omitted per F-NEIGHBOURHOOD-NOT-ON-MLS-LISTINGS) + `geoChanged` + `collectIdsForResolve` (Event 5 includes new rows, Event 6 includes geo-changed rows, skips unchanged).
- `scripts/smoke-landing-2-sync-hooks.js` -- harness with T1 RPC integration (BEGIN/ROLLBACK), T2 algorithm unit test, T3 backfill `.select('id')` RETURNING-id shape.

**D4 superseded (locked decision correction):** `territory-constants.ts` was originally locked at `scripts/lib/` per Landing 2 plan decision #4. tsconfig audit found `scripts/` is excluded from the TypeScript compilation graph (`tsconfig.exclude: ["node_modules", "scripts"]`), so the 3 Next.js-tree surfaces (`lib/homes-sync/save.ts`, `lib/building-sync/save.ts`, `app/api/admin/bulk-discovery/db-assign/route.ts`) could not have imported from `scripts/`. Canonical was moved to `lib/utils/territory-constants.ts`. Single canonical file; no inlined literals. All 6 surfaces import from the same source.

**Failure posture (additive, never aborts):** every reresolve RPC call is wrapped in a try/catch that logs and swallows errors. If reresolve fails on any surface: the upsert / backfill UPDATE remains committed, the cache stays NULL for those rows, Phase 2 readers fall through to the live resolver, and the sync proceeds. Hook NEVER throws and NEVER aborts the sync. Verified explicitly in the diff review.

**`backfillListingGeoIds` widening (4 copies):** return type changed `Promise<void>` -> `Promise<string[]>`. All 6 callers (3 lib-side + 3 inline-self-callers) discard the return value via bare `await fn();` -- verified pre-write. Widening is backwards-compatible.

**Smoke T1-T3 all PASS, rollback independently verified:**
- T1 (RPC integration): runtime-SELECTed NULL-cache routable listing `0ac95926-...` resolved inside BEGIN to `agent = fafcd5b1... (King Shah), scope = floor`. Post-smoke probe confirmed the row is back to `agent = NULL, scope = NULL` (ROLLBACK fired).
- T2 (algorithm): Event 5 + Event 6 + skip-unchanged + building_id change + NULL-vs-NULL sub-cases all correct.
- T3 (backfill `.select('id')`): runtime-SELECTed building `3a188ae4-...` UPDATE RETURNING id returned 2790 ids matching the building's listing count; ROLLBACK preserved the row population (verified by independent count post-smoke = 2790).

**tsc clean x2:** `npx tsc --noEmit --skipLibCheck` (project default, Next.js tree) exit 0; ad-hoc `tsc` against the 3 scripts-side files (override `tsconfig.exclude`) exit 0. SupabaseClient import in `geo-diff.ts` and all 6 territory-constants imports resolve cleanly.

**Backups:** 6 timestamped backups (`ts=20260530_060145`) preserved on disk for all 6 edited files (gitignored).

**Frozen contract preserved:** `resolve_agent_for_context` signature + body untouched. `reresolve_listings_in_set` is the only PG entry point the sync layer calls.

**Acceptance status:** the primitive layer of F-RESOLVE-AT-INSERT-PRIORITY closed by `2ca82c6` (v22); the sync-hook layer closed by `433c553` (v23). End-to-end "next-morning NULL-cache drop after one nightly sync" remains BLOCKED-ON-F-NIGHTLY-SYNC-TIMEOUT-6H (nightly sync still hits the 360-min GH Actions cap every night; ops fix is a separate ticket). In-the-meantime acceptance proxies (admin-homes sync route exercising the wired path; local smoke harness exercising the algorithm + RPC) both PASS.

- **Landing 3 Event 4 — SHIPPED 2026-05-30 (`9255a18`).** Deactivation reflow now fires asynchronously when `agents.is_active` or `is_selling` flips true->false; the worker drains via `reflow_deactivated_agent(uuid, uuid)` which composes `reresolve_listings_in_set` after NULLing the coupled trio so the sticky guard doesn't block same-scope re-resolve.

  **3-step DDL chain (all in production, all in git via `9255a18`):**
  - **Step A** (sync trigger, superseded but in the audit trail) — original migration created `reflow_deactivated_agent` (SECURITY DEFINER + locked search_path), `handle_agent_deactivate` (SECURITY INVOKER, synchronous PERFORM), and `trg_agent_deactivate_reflow` (AFTER UPDATE OF is_active, is_selling). In-tx V3 reflowed all 441,066 of King Shah's listings inside a SAVEPOINT and rolled back cleanly. Post-COMMIT V3 failed under `service_role` with `permission denied for table territory_reroll_queue` — surfaced BOTH a runner-side probe bug AND the real production-path bug (next step).
  - **Step B** (async handoff, **Q1 REVISED sync->async**) — post-mortem of Step A's V3 failure: the admin route's `authenticator` role caps `statement_timeout = 8s` (not resettable via `SET LOCAL ROLE`), and all three current WALLiam agents (King Shah, Neo Smith, WALLiam seed) hold ~430k cache rows each (floor-pool members + their geo carves). A 430k-row synchronous reflow inside the 8s ceiling fail-closed-rolls-back every deactivation; admin UI couldn't deactivate any current agent. Q1's original sync justification ("close the misroute window") was already moot because Q2 reader-hardening closes the window separately, so async became both necessary AND safer. Step B extended `territory_reroll_queue.scope` CHECK to include `'agent'` and swapped `handle_agent_deactivate` body from sync PERFORM to `INSERT ... ON CONFLICT (tenant_id, scope, scope_id) WHERE status='pending' DO NOTHING`. Body stayed SECURITY INVOKER at this step.
  - **Step C** (secdef fix, closes **F-EVENT-4-ASYNC-PERMISSION-DENIED** HIGH) — Step B's post-COMMIT V3 ALSO failed under `service_role`: even the cheap INSERT into `territory_reroll_queue` raised permission-denied because `service_role` has zero grants on the queue (postgres-owner-only) and `handle_agent_deactivate` runs as INVOKER. Grants enumeration via `information_schema.role_table_grants` confirmed. Step C is one line: `ALTER FUNCTION public.handle_agent_deactivate() SECURITY DEFINER SET search_path = public, pg_temp` — class-mirror of Landing 1's `pick_floor_agent` flip. Function body unchanged; only security model + search_path altered. INVOKER->DEFINER flip is safe because the body has no `auth.uid()`/`current_user`/`current_setting`/dynamic SQL; `NEW.tenant_id` comes from the trigger context (not caller-supplied); ON CONFLICT target is per-tenant. Tenant-isolation review PASSED.

  **3-layer architecture as shipped:**
  1. **Trigger:** `trg_agent_deactivate_reflow` AFTER UPDATE OF is_active, is_selling, WHEN clause matches only TRUE->(FALSE|NULL) transitions. Reactivation (FALSE->TRUE) and non-trigger column updates fire NO trigger.
  2. **Trigger function:** `handle_agent_deactivate()` (SECURITY DEFINER + locked search_path). NULL-tenant guard for platform-tier agents; INSERT into queue scoped to `NEW.tenant_id`; coalesces via ON CONFLICT.
  3. **Worker:** existing `app/api/admin-homes/territory/reroll-worker/route.ts` extended with `scope='agent'` branch dispatching to `reflow_deactivated_agent(scope_id, tenantId)`. pg-direct with `SET statement_timeout = 0` in place. NEW `.github/workflows/reroll-worker.yml` cron drains every 5 minutes across the configured tenant list, separate from `nightly-sync.yml` (closes **F-REROLL-WORKER-OPERATOR-PULL-ONLY**).

  **Q2 reader-hardening (the orthogonal safety half — closes `F-CACHE-FIRST-IGNORES-ACTIVE`).** The 7 Phase 2 cache-first reader sites (`lib/utils/tenant-resolver.ts`, `lib/actions/leads.ts`, and 5 API routes) now filter the cache-first INNER JOIN on `agents.is_active = true AND agents.is_selling = true` in addition to EDIT D's existing `agents.tenant_id` filter (`748ddd8`). Deactivated/not-selling agents become cache-miss and fall through to the RPC's tenant-scoped path. Holds correctness REGARDLESS of reflow latency: even if the cron is slow, a stale cache row pointing at a dead agent never routes a new lead to them.

  **Production-path gate (the bug-class-catcher) met THREE times.** Step A and Step B post-COMMIT V3s queried `territory_reroll_queue` directly under `SET LOCAL ROLE service_role`, which has no grant — failing for the WRONG reason and obscuring the real bug. Step C's migration V2 + apply-runner post-COMMIT V2 + a standalone T7 probe all use the corrected pattern (read queue under postgres, role-switch confined to the trigger-firing UPDATE, RESET ROLE before reading post-state). All three PASSED on real WALLiam agents (King Shah twice, WALLiam seed once); enqueue grew by 1 each time; rollback restored cleanly. **Smoke harness T1-T7** mirrors the same pattern; T7 is the bug-class-catcher.

  **Harness failure + clean-state verification.** Smoke T6 ("worker drain end-to-end" on King Shah's 441k listings) crashed with `Connection terminated unexpectedly` mid-reflow — the pg client lost its TCP connection to Supavisor session pooler. No `.on('error')` handler was attached so the Node process died. Filed as **F-SMOKE-CONNECTION-DROP-ON-LARGE-REFLOW**. Post-crash read-only verification confirmed: King Shah `is_active=true, is_selling=true`; no orphan `processing` or `pending` queue row; holdings restored to exact 441,066; coupled-CHECK invariant intact DB-wide. PostgreSQL's automatic rollback on connection loss covered for the harness bug. NOT a production-affecting issue; the worker route attaches `.on('error')` handlers and uses pg-direct with `statement_timeout=0`.

  **Cron auth surface (operator action pending).** The cron POSTs the worker route with a `Bearer ${REROLL_WORKER_CRON_TOKEN}` header. Worker route's `resolveTenantId` was extended with a fail-closed bypass: only activates if the env var is set AND >=32 chars AND the header matches exactly via `===`; requires BOTH the token AND a valid `tenant_id` query param; downstream queries all filter `WHERE tenant_id = $1` so cross-tenant access is impossible. **PENDING operator action before the cron can run:** set GH Actions secrets `REROLL_WORKER_BASE_URL` + `REROLL_WORKER_CRON_TOKEN`, AND set `REROLL_WORKER_CRON_TOKEN` as a server-side env var on the Next.js deployment.

  **GAP-5 honored.** Reflow writes `mls_listings` only; `leads.agent_id` is touched only by explicit user-initiated reassign/claim routes. Smoke T2 verified the pre-stamped lead's `agent_id` unchanged after agent deactivation.

- **Landing 3 Event 7 — nightly reconcile. OPEN.**
  - New `reconcile_corrections` table (per GAP-4) + GitHub Actions cron workflow (pg_cron is not installed on this Supabase project; reconcile must run external).
  - Threshold alert: > 50 corrections/run signals a missing mutation trigger.

---

# PART 4 — CONDENSED HISTORY

- **v1–v10 (2026-05-26/27):** P1 through P5.3 shipped. P5.3 (per-property-type resolved owner + source tier in GeographyView) CLOSED at commit `658ab62`.
- **v11–v13 (2026-05-27):** P-FLOOR opened. 3-round read-only recon resolved 9 Q-blocks. Confirmed: resolver terminates at `RETURN NULL` (no live P9/P10 fallback); `pick_routing_agent_for_type` honours condo/homes flags but not buildings; WALLiam apa = 1 muni + 11 community carves (deliberate, not phantom); Syed Shah is platform-tier (tenant_id NULL). Design D1–D6 locked (later generalized in v16).
- **v15 (2026-05-27):** Narrow floor migration APPLIED (18/18 verification checks, committed): `tenant_floor_pool`, `tenant_floor_alerts`, `pick_floor_agent`, resolver floor branch, `reroll_listings_at_floor`, audit/queue triggers, RLS, CHECK extensions. WALLiam pool seeded (3 agents; audit fired 3×; queue deduped to 1). Condo backfill ran (493,827 rows, even 3-way split) — to be reverted under v16. Homes backfill died mid-run (connection drop). BOM-strip fix applied to apply-runner.
- **v16 (2026-05-28):** Comprehensive long-term model LOCKED (Part 1). Narrow floor reframed as the unified routing-set primitive. Materialized-on-demand chosen over live-resolver. Sticky bindings, precedence-by-stored-scope, 7-event lifecycle defined. Two diagrams produced (resolution hierarchy, routing lifecycle). v15 reconciliation recorded (Part 2). GAP guards 2–6 added; cold-start checklist + mandatory tracker-update step (7) added to the EXECUTION PROTOCOL. Launch-readiness checklist (Part 7) and dashboard/launch-readiness gaps (GAP-7, GAP-8) recorded.
- **v17 (2026-05-28):** Cold-start investigation found the v15 homes backfill DB-committed despite the client logging "connection died mid-run" — a client-disconnect-after-commit. 799,688 homes were filled by `reroll_listings_at_floor`, not NULL as v15 recorded. P-REVERT-BACKFILL scope corrected to ~1.29M (495,428 condos + 799,688 homes). LESSON: a committed-server-side / failed-client-side transaction is why tracker memory and live DB diverged — the cold-start DB check is what caught it.
- **v18 (2026-05-28):** Phase 1 migration package finalized after recon + plan + 4 plan-review fixes + 3 SQL fixes (em-dashes, V7a tightened, V7d coverage diagnostic) + 2 down-migration fixes (em-dashes, NULL-safe NOT EXISTS). Three files on disk: up-migration (561 lines), down-migration (129 lines), apply-runner (427 lines). All reviewed, ASCII-clean, gated by `Bash(node scripts/apply-*)` ask permission. Pre-execution credential leak surfaced via Grep output_mode on `.env.local` — password rotated, lesson captured. NOT executed yet; pending operator approval after Supabase compute upgrade.
- **v19 (2026-05-29):** Phase 1 SHIPPED. DB-COMMITted on attempt 4 after three rolled-back attempts (V2 coupled-CHECK pre-population fixed via NOT VALID + later VALIDATE; V5a empty-target-set fixed via vacuous-pass Option C; direct-host DNS unresolvable for this project fixed by switching to session pooler + relaxing runner pooler-heuristic to port 6543 only). 89 min wall time. Cache verified v16-correct: 12,621 Whitby community listings → King Shah @ scope='community' (11/11 carves clean); 2 Commercial pins preserved; 1,284,892 condo/home at scope='floor' (3-way hash split, delta < 0.06%); V3 coupled-state invariant holds. 63 post-COMMIT smoke "regressions" identified as false positives (v15→v16 expected transitions + a baseline-capture bug in the runner). 34 NULL condo/home rows accumulated in 1h38m post-COMMIT — confirms resolve-at-insert as the live gap (F-RESOLVE-AT-INSERT-PRIORITY, elevated). Four lessons captured in PART 6.
- **v20 (2026-05-29):** Phase 2 SHIPPED — cache-first reader wiring complete across 7 files (wrapper + leads helper + 5 API routes), with tenant-scope fix. 5 commits: EDIT A `7b5a59c`, EDIT B `4170ba6`, EDITs C.1–C.4 `cfc3710`, EDIT C.5 `da86f13`, EDIT D `748ddd8`. New finding **F-PHASE2-CACHE-NOT-TENANT-SCOPED** surfaced in E2E smoke S3 (cross-tenant cache leak: aily POST against a WALLiam-cached listing returned a WALLiam agent through the cache-first shortcut, because the initial preamble keyed on `listing_id` alone with no tenant filter) and **closed in the same phase via EDIT D** (cache-first INNER JOIN on `agents.tenant_id` filtered by caller's tenant_id). Empirical fix-proof on same call shape (aily POST + listing `f8a24890`): pre-EDIT-D `agent_id = King Shah` → post-EDIT-D `agent_id = NULL`. **F-FLOOR-POOL-PERMISSION-DENIED** filed as a bounded pre-existing bug (only reachable via NULL-cache path through `assign-user-agent`; resolved as a side-effect by P-LIFECYCLE's resolve-at-insert hook). 13 smoke leads purged in one strict-asserted transaction. The "9 production callers" count from v18 corrected to 13 + 1 placeholder (7 cache-first eligible, 6 RPC-only). Three lessons captured in PART 6.
- **v21 (2026-05-29):** P-LIFECYCLE Landing 1 SHIPPED — `pick_floor_agent` flipped to SECURITY DEFINER with locked `search_path = public, pg_temp`. Closes **F-FLOOR-POOL-PERMISSION-DENIED** earlier than v20 anticipated (v20 expected it would be resolved as a side-effect of Landing 2's resolve-at-insert; Landing 1 instead addresses the grant layer directly, surgically, and ships independently). Recon (`phase-lifecycle-recon-output.txt`) confirmed Events 1-3 already covered by Phase 1's apa AFTER triggers, narrowing P-LIFECYCLE active scope to Landings 1-3 (grant fix, resolve-at-insert + geo-diff, deactivation + reconcile). Preconditions PASSED (both): (a) zero application code calls `pick_floor_agent` directly - only DB-internal callers (`resolve_agent_for_context`, `reroll_listings_at_floor`), both SECURITY INVOKER, both honor the validated-tenant-id contract via Phase 2 EDIT D's audit; (b) `pick_floor_agent` body uses no `auth.uid()` / `current_user` / `session_user` / `current_setting` - purely parameter-driven, so the INVOKER->DEFINER->INVOKER chain doesn't break caller auth context. Empirical fix-proof inside the same transaction: V4 `SET LOCAL ROLE service_role; SELECT pick_floor_agent(...)` returned Neo Smith (`f2ce3011-...`); V5 escalation through `resolve_agent_for_context` confirmed end-to-end. 1 commit: `4e69773`. Rollback-snapshot captured pre-BEGIN. NULL-cache growth measured at ~15.2/hour steady (~360/day) - elevates Landing 2 (resolve-at-insert + geo-diff) as next priority. One lesson captured in PART 6.
- **v22 (2026-05-29):** P-LIFECYCLE Landing 2 primitive SHIPPED + git-vs-production drift reconciled. **Landing 2 commit `2ca82c6`** introduces `reresolve_listings_in_set(uuid[], uuid)` as a SECURITY DEFINER set-based primitive: 10-level cascade (pin -> building -> community/condo+home -> municipality/condo+home -> area/condo+home -> floor/condo+home) narrowed by `ml.id = ANY(p_listing_ids)`, writes provenance trio atomically, hash-RR via `PARTITION BY apa.<geo>_id` for multi-bucket batches. Tenant isolation hardened pre-COMMIT after a 10/10 audit caught that L3-L10 lacked the `agents.tenant_id = p_tenant_id` belt-and-suspenders predicate (only the apa/tfp anchor was scoped); fix added 10 `a.tenant_id` predicates before apply. NULL tenant now no-ops via the predicates themselves (early guard's `p_tenant_id IS NULL` clause removed). `reresolve_listing` patched to delegate, closing **F-RERESOLVE-COUPLED-CHECK** (latent bug: old body UPDATEd `assigned_agent_id` only, violated Phase 1 coupled CHECK on NULL-cache rows). V1-V6 in-tx asserts PASS; T1-T5 smoke PASS; apply-runner V3 had a hoist bug that put the tenant-pick SELECT under `service_role` (no grant on `tenant_floor_pool`) which I caught, fixed, and standalone re-verified. Down-migration is DROP-only; new down-runner reads the original body from the rollback snapshot (no hardcoded body anywhere in the down path). Walk-equivalence holds modulo N=1 carves (this function hash-RRs, resolver primary-picks; agree at N=1, diverge by design at N>1 per v16 distribution-vs-assignment split). Frozen contract preserved (resolve_agent_for_context untouched). **Two reconciliation commits closed git-vs-production drift:** `4a9059e` backfilled 12 already-applied artifacts (P-FLOOR migration, Phase 1 up/down, Phase 1 apply-runner, 8 rollback snapshots) that v19 noted as drifted; `dea6d71` added the previously-untracked `CLAUDE.md` (project standing instructions) and gitignored `.claude/` (per-machine `settings.local.json`). Branch ahead of `origin/main` by 5 commits (`4e69773` Landing 1, `d1d7d4f` v21, `2ca82c6` Landing 2, `4a9059e` backfill, `dea6d71` CLAUDE.md/gitignore), not pushed. Remaining Landing 2 work (TypeScript sync hooks + geo-diff util + tenant-constant file) is BLOCKED-ON-F-NIGHTLY-SYNC-TIMEOUT-6H for end-to-end acceptance. Six lessons captured in PART 6.
- **v23 (2026-05-30):** P-LIFECYCLE Landing 2 sync hooks SHIPPED — `reresolve_listings_in_set` wired into all 6 production listing-ingestion surfaces (Events 5 + 6). Commit `433c553`: 9 files changed, 3 new (`lib/utils/territory-constants.ts`, `lib/utils/geo-diff.ts`, `scripts/smoke-landing-2-sync-hooks.js`), 6 edits across both homes copies + four `backfillListingGeoIds` copies. Coverage corrected from the planning recon's 4 surfaces to 6 after a pre-write duplication audit found `backfillListingGeoIds` in two more production paths (`scripts/bulk-discover-assign.ts`, `app/api/admin/bulk-discovery/db-assign/route.ts`); both wired before any write. **Locked decision D4 superseded:** `territory-constants.ts` moved `scripts/lib/` -> `lib/utils/` because `tsconfig.exclude: ["scripts"]` made the 3 Next.js-tree surfaces unable to import from `scripts/`. Single canonical file; no inlined literals. **Additive failure posture:** every reresolve RPC call is try/catch-wrapped; on error the upsert/backfill stays committed, the cache stays NULL for those rows, Phase 2 readers fall through. The hook never throws and never aborts the sync. **`backfillListingGeoIds` widened `Promise<void>` -> `Promise<string[]>`** (4 copies) with `.select('id')` to capture affected ids; all 6 callers discard the return (verified pre-write). **Frozen contract preserved:** `resolve_agent_for_context` untouched. **Smoke T1-T3 PASS** with rollback independently verified (zero DB state left). **tsc clean x2** (project default + ad-hoc scripts-side). 6 timestamped backups (`ts=20260530_060145`) preserved (gitignored). Closes F-RESOLVE-AT-INSERT-PRIORITY at the sync-hook layer (already RESOLVED at the primitive layer by v22). New finding **F-SYNC-LAYER-DUPLICATION** (OPEN, NOT BLOCKING) tracks the wire-all-6 reality + the homes-save / backfillListingGeoIds duplication to be consolidated in a separate cleanup commit. End-to-end NULL-cache-drop acceptance remains BLOCKED-ON-F-NIGHTLY-SYNC-TIMEOUT-6H (nightly sync still hits the 360-min GH Actions cap). Branch ahead of `origin/main` by 2 commits after this v23 tracker commit (sync hooks `433c553` + this v23 tracker), pending push.
- **v24 (2026-05-30):** F-NIGHTLY-SYNC-TIMEOUT-6H opened as an ACTIVE track and Fixes 1+2+4 APPLIED — operational ticket previously blocking end-to-end Landing 2 acceptance moved from OPEN to ACTIVE/awaiting-confirmation. Commit `c77ce01` touches exactly 2 files: `scripts/sync-buildings-incremental.ts` (`DELAY_BETWEEN_BUILDINGS_MS` 2000 -> 200 with provenance comment citing origin commit `0a104af` having no 429 incident; buildings loop ordering `building_name` -> `last_synced_at` ASC NULLS FIRST for fair rotation) and `.github/workflows/nightly-sync.yml` (single 360-min job -> two-job sequential split `homes-sync` -> `buildings-sync` via `needs:` + `if: always()`, each with its own 360-min budget; deliberate timing note recorded inline that worst-case wall-clock now spans both budgets ~12h end-to-end so a future session does not mistake the structure for concurrent jobs). Backups for both files timestamped `20260530_072340` (gitignored). tsc `--noEmit` clean + YAML-lint PASS against the repo-resident workflow. The 3 pre-existing local modifications (`app/api/charlie/municipalities/route.ts`, `scripts/r-w-territory-master-p2-data-phantom-fix.js`, `scripts/r-w-territory-master-p4-check-fix.js`) stayed unstaged and untouched. **Fast-path eligible** (pattern-proven phase: small low-risk edits to a proven pattern, no new multi-tenant function, no frozen-contract change). **Fixes 3 (parallel homes path) and Fix 5 (raise `timeout-minutes` above 360)** remain deferred sub-items; revisit only if 1+2+4 prove insufficient. **Resolution NOT claimed** — gated on tomorrow's `sync_history` showing non-zero buildings completions; verify before close. Closure of F-NIGHTLY-SYNC-TIMEOUT-6H also clears the only remaining blocker on F-RESOLVE-AT-INSERT-PRIORITY's end-to-end production validation.
- **v25 (2026-05-30):** P-LIFECYCLE Landing 3 Event 4 SHIPPED — deactivation reflow LIVE on production, async via `territory_reroll_queue` + every-5-min cron drain. Commit `9255a18`: 21 files, 2,791 insertions across a 3-step DDL chain (sync -> async -> secdef) + Q2 reader-hardening + worker route extension + new GH-Actions cron workflow + T1-T7 smoke harness. **Q1 formally REVISED sync -> async** after post-mortem of Step A's apply showed the admin route's `authenticator` role caps `statement_timeout = 8s` (not resettable via `SET LOCAL ROLE`) and all three current WALLiam agents hold ~430k cache rows each — synchronous reflow inside 8s fail-closed-rolls-back every deactivation. Q2 reader-hardening (the orthogonal half: cache-first INNER JOIN now also filters `agents.is_active = true AND agents.is_selling = true` across all 7 reader sites) is what made async safe — stale cache rows pointing at a dead agent fall through to the RPC regardless of cron latency. **Step C (secdef fix)** closes **F-EVENT-4-ASYNC-PERMISSION-DENIED** (HIGH) — `handle_agent_deactivate` was authored SECURITY INVOKER; `service_role` has zero grants on `territory_reroll_queue` (postgres-owner-only), so trigger INSERT failed permission-denied when fired from the admin route. One-line `ALTER FUNCTION ... SECURITY DEFINER SET search_path = public, pg_temp` (class-mirror of Landing 1's `pick_floor_agent` flip). **Production-path gate (service_role enqueue must succeed)** met THREE times: in-migration V2 + apply-runner post-COMMIT V2 + standalone T7 probe; all three used the LESSON pattern (queue reads under postgres, role-switch confined to the trigger-firing UPDATE) which is the **second occurrence** of the apply-runner-V3-service-role-probe class (Landing 2 was first; lesson now captured in PART 6). **Worker route** extended with cron Bearer-token bypass (fail-closed: requires env var set AND >=32 chars AND exact `===` match AND a valid `tenant_id` query param; downstream queries all `WHERE tenant_id=$1`). **Cron workflow** is its own file (`.github/workflows/reroll-worker.yml`), separate from `nightly-sync.yml` per F-NIGHTLY-SYNC-TIMEOUT-6H caution. **Smoke T6 (441k drain) crashed mid-reflow** with `Connection terminated unexpectedly` (no `.on('error')` handler); filed as **F-SMOKE-CONNECTION-DROP-ON-LARGE-REFLOW**. Post-crash read-only verification confirmed zero state left (King Shah is_active=true, no orphan queue rows, exact 441,066 holdings, coupled-CHECK intact DB-wide) — PostgreSQL's automatic rollback on connection loss covered for the harness bug. **PENDING operator action** before the cron can fire: set GH secrets `REROLL_WORKER_BASE_URL` + `REROLL_WORKER_CRON_TOKEN`, AND set the cron token as a server-side env var on the Next.js deployment. **7 findings recorded in PART 5** (F-EVENT-4-ASYNC-PERMISSION-DENIED RESOLVED, F-CACHE-FIRST-IGNORES-ACTIVE RESOLVED, F-REROLL-WORKER-OPERATOR-PULL-ONLY closed by cron, F-EXISTING-HANDLE-APA-TRIGGERS-SAME-LATENT-RISK sweep, F-SMOKE-CONNECTION-DROP-ON-LARGE-REFLOW, F-CRON-TOKEN-NOT-CONSTANT-TIME, F-QUEUE-DUPLICATE-INDEX cleanup). **4 lessons captured in PART 6.** Branch ahead of `origin/main` by 1 commit (`9255a18`) before this v25 tracker commit; both pushed together.
- **v27 (2026-05-31):** P-DASHBOARD CORE-5 SHIPPED — five operator-cockpit gaps closed in one bundle. Commit `3a4167a`: 7 files, +396/-39, zero new files (all ADJUST + EXTEND of the existing 7-view cockpit), no DDL, no new multi-tenant function, no frozen-contract change. **GAP-A** (discoverability): `/admin-homes/territory` previously rendered the legacy pilot TerritoryClient (the operator's expected URL landed on dead UI); now redirects tenant users to `/admin-homes/tenants/<id>` (the actual operator cockpit) and platform admins to `/admin-homes/tenants`. Legacy TerritoryClient still reachable as the "Detail" sub-view inside the cockpit's TerritoryTab. **GAP-E** (agent footprint + reflow-preview safety): AgentsView now shows true `mls_listings.assigned_agent_id` footprint per agent (was card-count only -- King Shah's row showed 11 cards, real footprint is 441,066 cache rows). Bold-amber highlight when footprint >= 10k. The reassign and deactivate confirmation dialogs both preview the reflow size so an operator can no longer blindly trigger a 441k-row reflow via Event 4's queue. Footprint sourced via pg-direct as `postgres` (2-min ceiling) rather than supabase-js -> PostgREST (8s authenticator cap that would flake on the 1.3M-row GROUP BY scan). **GAP-B** (floor-pool alerts surface): HealthView reads `tenant_floor_alerts` and surfaces both the count (as a ProblemRow) and a detail list (when non-empty) of `empty_floor_pool` and similar alerts written by Landing 1's `pick_floor_agent` and Event 4's `reflow_deactivated_agent`. WALLiam currently has 0 alerts (genuine); aily has 1 legacy alert from 2026-05-27. **GAP-C** (cron observability): QueueIndicator extended with last-drain-time, last-1h activity (done + error counts), last-error message + age, all sourced from `territory_reroll_queue.processed_at / error_message / status` via pg-direct. Pending pill escalates from amber ("cron healthy, just behind") to red ("cron stalled -- 0 drains in last hour") based on `recent_done_count`. Synced pill shows "Synced, N recent err" when errors fired in the last hour. **GAP-D** (NULL-cache visibility): HealthView surfaces global `mls_listings WHERE assigned_agent_id IS NULL` count (system-wide -- mls_listings has no tenant_id column) with property-type breakdown (condo + home + other). Live state at v27 commit: 3,762 total / 968 condo / 1,993 home / 819 other. Surfaces F-RESOLVE-AT-INSERT-PRIORITY's daily drift to operators. **Pattern**: the route-side reads for GAP-B + GAP-D both initially used supabase-js -> service_role and BOTH soft-failed silently to `0`/`[]`, producing a false-green render that looked identical to a healthy zero state. Two distinct failure modes confirmed by post-build smoke: (1) **F-TENANT-FLOOR-ALERTS-SAME-GRANT-WALL** -- `tenant_floor_alerts` is the third sibling table (after `tenant_floor_pool` from Landing 1, `territory_reroll_queue` from Event 4) with postgres-only grants + broken auth.uid()-keyed RLS. service_role returned 42501 permission-denied. (2) **F-POSTGREST-COUNT-EXACT-SILENT-500** -- supabase-js `.select('id', { count: 'exact', head: true }).is('assigned_agent_id', null)` against the 1.3M-row mls_listings table returned status 500 with empty error body. Direct SQL under `SET LOCAL ROLE service_role` for the same query succeeded in <100ms; the issue is PostgREST-specific (likely the 8s authenticator timeout, returned as a 500 with no body). **Both fixes**: switched the two reads to pg-direct as `postgres` (mirrors GAP-C's pattern); failed reads now return `null` (not `[]`/`{0,...}`) and the frontend renders a visually-distinct red "could not read" panel. **F-FALSE-GREEN-VIA-SILENT-SOFT-FAIL** captured as a PART 6 lesson: a health-signal read that defaults to 0/empty on failure is indistinguishable from a healthy zero and HIDES the problem the view exists to surface. **Test-design discipline reused**: F-apa-secdef-sweep V3 test-design bug from v26 (`is_primary` vs `is_active`) was re-applied here -- before committing CORE-5 we verified the pg-direct queries return real data via end-to-end route simulation (RPC + pg-direct extensions + render-verdict prediction matched browser observation). Operator click-through screenshot confirmed gray panels, real numbers, no red, no console errors. Branch ahead of `origin/main` by 1 commit (`3a4167a`) before this v27 tracker commit; both pushed together. GAP-7-DASHBOARD-UNDERSPECIFIED closed.

- **v26 (2026-05-30):** F-apa-secdef-sweep SHIPPED — `handle_apa_insert/update/delete` flipped INVOKER -> SECURITY DEFINER + locked `search_path = public, pg_temp`, closing **F-EXISTING-HANDLE-APA-TRIGGERS-SAME-LATENT-RISK** (HIGH-priority sweep target recorded in v25). Commit `d2f0e69`: 5 new files (migration up+down, apply runner, down runner, smoke harness) + 1 rollback-snapshot of pre-flip handler bodies; zero edits to existing files. **Third instance of the same surgical SECDEF pattern** -- class-mirror of Landing 1's `pick_floor_agent` flip (`4e69773`, v21) and Event 4 Step C's `handle_agent_deactivate` flip (Step C of v25's `9255a18`). Proactive (not urgent): all 7 current APA write paths use pg-direct as `postgres` (which has full grants on `territory_reroll_queue`), so no production traffic was actively broken; sweep done now while the class-pattern was fresh from Event 4. `apa_mutation_lock_trigger` STAYS INVOKER (body verified in recon: only `pg_advisory_xact_lock`, no table writes, no grants needed). **Inner-function inheritance proven empirically by V5 (in-tx) + T4 (smoke):** SYNC-path INSERT under `service_role` (no `app.skip_apa_reroll` GUC) exercises the full chain incl. `PERFORM reroll_listings_at_geo`; the inner INVOKER function inherits postgres privileges through the DEFINER chain. **Test-design bug caught and fixed mid-apply:** initial V3 used `UPDATE ... SET is_primary = TRUE` (not a routing field per `v_routing_changed` predicate -- correctly a no-op, function returned early without enqueueing). Migration rolled back cleanly, NO production state changed (atomic PG tx); V3 + T2 fixed to `SET is_active = FALSE` (a verified TRUE->FALSE transition against setup state which inserts with `is_active=TRUE`); re-audited V2-V5/T1-T5 to confirm each assertion exercises an enqueue-triggering mutation before re-apply. Production-path gate (`service_role` APA INSERT/UPDATE/DELETE enqueues) met in V2-V4 in-tx + T1-T3 smoke; inheritance gate (V5 + T4) met. Forensic snapshot of pre-flip bodies preserved at `supabase/migrations/rollback-snapshots/_f-apa-secdef-sweep_handlers_2026-05-30T20-17-56-548Z.sql` (down-runner doesn't use it -- bodies unchanged across up/down). Failed-apply snapshot (`_f-apa-secdef-sweep_handlers_2026-05-30T20-10-58-658Z.sql`) deliberately left LOCAL (not committed) -- maps to no production state since that apply rolled back. Branch ahead of `origin/main` by 1 commit (`d2f0e69`) before this v26 tracker commit; both pushed together.

---

# PART 5 — OPEN FINDINGS / INDEPENDENT ITEMS

- **F-WALLIAM-CREDS-PLACEHOLDER-IN-DB (RESOLVED for WALLiam 2026-06-01, v5 reopen `4cf4eed`)** — Original observation: during v15 recon, a `SELECT *` on `tenants` leaked WALLiam's Anthropic + Resend keys to a chat transcript; two subsequent placeholder UPDATEs overwrote the real keys with literal placeholder strings (`REPLACE_…_KEY`). **RESOLUTION:** v5 reopen wired real keys with full validation evidence: `tenants.resend_api_key` = `re_BJJ...cqSr` (fp len 36) verified against Resend `GET /domains` 200 OK + 1 verified domain; `tenants.anthropic_api_key` = `sk-ant...zwAA` (fp len 108) verified against Anthropic `GET /v1/models` 200 OK + 10 models. CV-EMAIL real send to `delivered@resend.dev` returned Resend message id `0a05149f-7429-4da4-ad8e-8b6a90449b58`. CV-EST real $561K valuation against live Whitby comparables, with real Sonnet 4 commentary (msg id `70bdb36d-...`). Per-agent AI wiring (`agents.ai_estimator_enabled=TRUE` + per-agent `anthropic_api_key`) applied to all 3 WALLiam agents (King Shah / Neo Smith / WALLiam seed) — closes the orthogonal F-ESTIMATOR-AI-READS-AGENT-KEY-NOT-TENANT-KEY finding for WALLiam (the estimator reads `agents.anthropic_api_key`, not `tenants.anthropic_api_key`). Persistent launch config verified to survive CV fixture teardown. **Aily caveat (NOT blanket-resolved):** Aily's tenant API keys are wired to the same real values, but `tenants.resend_verification_status = NULL` for Aily AND its `send_from` / `email_from_domain` still point at `condoleads.ca`/WALLiam-flavored. `sendTenantEmail`'s pre-flight will throw `TenantEmailNotConfigured` for any Aily send until the operator verifies Aily's own sending domain in the Resend dashboard + updates `send_from`/`email_from_domain` to an Aily-owned identity. So WALLiam-launch path is clear; Aily-launch path is BLOCKED-ON-DOMAIN-VERIFICATION.
- **F-CASCADE-BUILDINGS-ACCESS-IGNORED** — `pick_routing_agent_for_type` honours condo/homes flags but has no buildings parameter; `buildings_access` on apa rows is currently decorative. Decide whether building-tier carving needs the flag wired (relevant to P-ROUTING-SET).
- **F-AUDIT-ORIGINATOR-WRITE-GAP** — partially fixed for the floor-pool surface (`handle_tenant_floor_pool_change` writes `changed_by = auth.uid()`). Other mutation surfaces may still not record originator. Sweep before launch.
- **F-HOMES-FILLED-UNEXPECTEDLY (RESOLVED 2026-05-28 — confirmed v15 backfill committed despite client disconnect)** — Cold-start check 2026-05-28 found 799,688 of 801,325 homes (99.8%) have `assigned_agent_id` filled, contradicting the tracker's claim that homes were never backfilled ("connection died mid-run — already clean NULL"). ~787K filled home rows are unaccounted for. Source unknown — three candidates: (a) the v15 homes backfill actually completed/partial-committed further than logged, (b) an unrecorded later backfill ran, (c) a trigger or the nightly sync is filling homes on insert/update. Changes P-REVERT-BACKFILL scope: currently ~493,827 condos; if homes are also listing-level picks, scope is ~1.29M, and GAP-3's down-migration must account for them. RESOLUTION: read-only investigation FIRST. Do NOT write any migration, amend the revert scope, or correct PART 2's homes number until the source is identified. RESOLVED: source is the v15 reroll_listings_at_floor homes run; same listing-level pick_floor_agent picks as condos. Revert scope corrected to ~1.29M (495,428 condos + 799,688 homes).
- **F-AILY-CROSS-TENANT-SMOKE-DEFERRED** — V6 cross-tenant isolation assertion is structurally correct but vacuous because aily has no routing data today. Real smoke deferred until aily has actual agents + routing-set rows. Do NOT add synthetic aily agents to the production agents table via migration SQL (credentials-bearing table).
- **F-RESOLVE-AT-INSERT-PRIORITY (RESOLVED 2026-05-30 at code layer, Landing 2 sync hooks `433c553`; end-to-end production validation remains BLOCKED-ON-F-NIGHTLY-SYNC-TIMEOUT-6H)** — 34 NULL condo/home rows accumulated in 1h38m between Phase 1 COMMIT (2026-05-29 10:04 UTC) and the post-COMMIT verification probe. Cause: no resolve-at-insert hook -- `assigned_agent_id` was left NULL on every new MLS row inserted by nightly sync (verified in P-MODEL-recon Q5: `lib/homes-sync/save.ts` does not write the column [path corrected from v21's "`lib/homes-save.ts`"]; no insert trigger fills it). Confirmed decision-A (resolve-at-insert) from the v16 model as the active drift source producing daily NULL accumulation. **Primitive-layer RESOLUTION (Landing 2 primitive, `2ca82c6`):** `reresolve_listings_in_set(uuid[], uuid)` shipped as the set-based resolver. V1-V6 in-tx + T1-T5 smoke proved correctness. **Sync-hook RESOLUTION (Landing 2 sync hooks, `433c553`):** the primitive is now wired into all 6 production ingestion surfaces (both homes save copies + four `backfillListingGeoIds` copies). New listings resolve at insert (Event 5); geo-changed listings re-resolve on backfill (Event 6). Failure-additive (try/catch wrap, never aborts sync). T1-T3 smoke PASS with rollback verified. F-RESOLVE-AT-INSERT-PRIORITY is closed at the code layer. **End-to-end production validation (next-morning NULL-cache drop after one nightly sync)** remains BLOCKED-ON-F-NIGHTLY-SYNC-TIMEOUT-6H -- the nightly sync still cancels at the GH Actions 360-min cap every night, so the wired hooks can't yet be observed in production at end-to-end scope until the operational timeout fix lands. Acceptance proxies (admin-homes sync routes exercising the wired path; local smoke) both PASS.

- **F-SYNC-LAYER-DUPLICATION (OPEN, NOT BLOCKING, tracked for separate cleanup)** — Surfaced during Landing 2 sync-hooks pre-write audit. The sync save layer exists in **two parallel copies for homes** (`lib/homes-sync/save.ts` 946 lines + `scripts/lib/homes-save.ts` 949 lines, near-identical) and **four parallel copies of `backfillListingGeoIds`** (in `lib/building-sync/save.ts`, `scripts/sync-buildings-incremental.ts`, `scripts/bulk-discover-assign.ts`, `app/api/admin/bulk-discovery/db-assign/route.ts`). Landing 2 sync hooks wired all 6 surfaces (per operator decisions D1 + D3) rather than block on consolidation; coverage is uniform but drift risk between copies remains. **Consolidation deferred** to a separate cleanup commit (possibly part of `scripts/_cleanup-scripts-dir.js`'s in-flight sweep). Risk mitigation: Landing 2's hook diffs are identical across copies (same RPC call, same try/catch, same WALLIAM_TENANT_ID import target), the smoke harness exercises the shared algorithm, and a future consolidation collapses all four backfills + both homes saves to single canonical implementations. Until then, any future change to the sync-hook pattern must update ALL 6 copies; the v23 commit message lists them explicitly as a future-author checklist.

- **F-RERESOLVE-COUPLED-CHECK (RESOLVED 2026-05-29, Landing 2 `2ca82c6`)** — Latent bug surfaced during Landing 2 recon. The pre-Landing-2 `reresolve_listing(uuid, uuid)` body did `UPDATE mls_listings SET assigned_agent_id = v_new_agent` and never wrote `assigned_scope`. After Phase 1 added the coupled CHECK `(agent NULL ⇔ scope NULL)`, ANY call to the old function on a NULL-cache row that resolved to a non-NULL agent would crash with `mls_listings_assigned_coupled_check`. Silent today because nothing in production called the function. Reproduced cleanly inside a `BEGIN/ROLLBACK` probe during recon (Section G of `phase-lifecycle-landing-2-recon-output.txt`). **RESOLUTION:** Landing 2 introduces `reresolve_listings_in_set` which writes the coupled trio atomically via single UPDATE per cascade level, and patches `reresolve_listing` to delegate to it with a one-element array. V6 in-tx + smoke T1 prove the patched single-row wrapper now lands the coupled trio without crashing.

- **F-VERIFY-READONLY-HANG (OPEN, low-priority)** — Surfaced in Landing 2 verify recon and reproduced in cold-start: a SELECT of the form `SELECT property_type, COUNT(*) FROM mls_listings WHERE assigned_agent_id IS NULL AND property_type IN (...) AND municipality_id IS NOT NULL GROUP BY property_type` hangs indefinitely when run as the SECOND query in the same `pg` Client connection (the first query completes in <2s). Same query in a FRESH client connection completes in ~1s. Cause not yet diagnosed; hypothesis is plan-cache state from the first query. **Mitigation:** smoke + recon harnesses open one `pg` Client per probe and close it; multi-probe single-connection harnesses are at risk. Not a Landing 2 blocker — Landing 2's UPDATEs run via the migration runner + future sync layer, not the verify harness.

- **F-SYNC-SINGLE-TENANT-IMPLICIT (OPEN, NOT BLOCKING)** — Surfaced in Landing 2 recon. The nightly sync (`.github/workflows/nightly-sync.yml` -> `scripts/nightly-sync.ts` -> `lib/homes-sync/save.ts` + `lib/building-sync/save.ts`) has NO tenant loop, no tenant parameter, no per-tenant decision; it implicitly assumes all listings belong to WALLiam. `scripts/smoke-recipients-helper.ts:34` hardcodes `WALLIAM_TENANT_ID = 'b16e1039-...'`. Landing 2's sync hook will pass WALLiam as a constant via the new `scripts/lib/territory-constants.ts`. Multi-tenant onboarding will need a per-listing tenant-derivation step (probably by geo). DO NOT extend the sync to multi-tenant inside Landing 2 — file this as a separate concern.

- **F-NEIGHBOURHOOD-NOT-ON-MLS-LISTINGS (CLARIFICATION, not a bug)** — `mls_listings` has NO `neighbourhood_id` column (confirmed in CLAUDE.md "Verified key IDs" + verified at Landing 2 plan time). `resolve_agent_for_context`'s P3 (neighbourhood) branch is therefore unreachable from any listing-context caller; the existing `reresolve_listing` also passed NULL for neighbourhood. Landing 2's `reresolve_listings_in_set` cascade correctly omits a neighbourhood-scope step; walk-equivalence with the resolver holds because neither walks neighbourhood from listing context. If neighbourhood data ever lands on `mls_listings`, the function gains an `L_NEIGHBOURHOOD` step between L2 (building) and L3 (community). Not a bug.

- **F-NIGHTLY-SYNC-TIMEOUT-6H (ACTIVE — fixes 1+2+4 applied 2026-05-30 `c77ce01`, awaiting first-night confirmation)** — Surfaced in Landing 2 verify recon via `gh run list --workflow=nightly-sync.yml --limit 8`: ALL 8 most recent runs (2026-05-21 through 2026-05-29) cancelled at exactly ~6h runtime — GH Actions' default 360-min job timeout. `nightly-sync.yml` did not set `timeout-minutes`, so the cap was the default 6h. Root causes (per `f-nightly-sync-timeout-recon-output.txt`): (1) `DELAY_BETWEEN_BUILDINGS_MS = 2000` × 9,835 buildings = 5.46h sleep alone, eating the budget before work could complete; (2) buildings loop ordered by `building_name` (alphabetical) starved late-alphabet rows every night when the cap hit mid-loop (sync_history showed zero buildings completions over 7 days despite all 9,835 rows having `last_synced_at` set); (3) single-job design forced homes + buildings to share one 6h budget. **Fixes 1+2+4 APPLIED 2026-05-30 (`c77ce01`):** (1) `DELAY_BETWEEN_BUILDINGS_MS` 2000 -> 200 (fetchWithRetry already handles 429 with Retry-After; the 2s blanket was redundant, no incident citation in git blame from origin commit `0a104af`); (2) buildings loop `.order('last_synced_at', { ascending: true, nullsFirst: true })` for fair rotation (never-synced first, then oldest-synced); (4) `nightly-sync.yml` split into `homes-sync` + `buildings-sync`, each with its own 360-min budget, sequential via `needs: homes-sync` + `if: always()` so buildings runs even if homes was cancelled at its cap (`inputs.skip_buildings` still overrides). Sequential not parallel because both phases UPSERT `mls_listings` on overlapping `listing_key` (condos appear in both feeds) and parallel doubles PropTx rate. **Timing change recorded inline in the YAML:** worst-case wall-clock now spans both budgets (~12h end-to-end across two jobs), acceptable for a 2 AM cron but a deliberate change a future session must not mistake for concurrent jobs. tsc + YAML-lint PASS. **Deferred sub-items:** Fix 3 (parallel homes path via the existing `app/api/admin-homes/parallel-sync/route.ts` semaphore pattern) and Fix 5 (raise `timeout-minutes` above 360) remain in scope but unapplied; revisit only if Fixes 1+2+4 prove insufficient. **OPEN ACTION (morning after next nightly run):** read `sync_history` and confirm non-zero buildings completions. NOT resolved until verified — a green CI run alone is not the acceptance signal. Closure also unblocks F-RESOLVE-AT-INSERT-PRIORITY's end-to-end production validation (currently the only remaining blocker for next-morning NULL-cache drop).

- **F-PHASE2-CACHE-NOT-TENANT-SCOPED (RESOLVED 2026-05-29, EDIT D `748ddd8`)** — Phase 2's initial cache-first preamble (EDITs A/B/C in commits `7b5a59c`, `4170ba6`, `cfc3710`, `da86f13`) read `mls_listings.assigned_agent_id` by `listing_id` only with no tenant filter, bypassing the tenant scoping that `resolve_agent_for_context.p_tenant_id` enforced on the path it shortcut. Surfaced in E2E smoke S3: an aily POST on WALLiam-cached listing `f8a24890` returned a WALLiam agent (King Shah) into an aily lead. Architectural blast radius significant (reintroduces the v15-era tenant-leak class); practical blast radius today zero (aily has no production traffic). **RESOLUTION:** EDIT D rewrote the cache-first SELECT across all 7 files to INNER JOIN `agents` with `agents.tenant_id = caller_tenant_id` — cross-tenant cached agents become cache-miss and fall through to the RPC's already-tenant-scoped path. Selects only `tenant_id` from `agents` (respects no-`SELECT *`-on-`agents` rule). FK hint syntax (`agents!mls_listings_assigned_agent_id_fkey!inner`) mirrors the existing pattern in the codebase. **Empirical fix-proof** (same listing `f8a24890`, same aily POST shape, same call): pre-EDIT-D `lead.agent_id = fafcd5b1... (King Shah)` → post-EDIT-D `lead.agent_id = NULL`. Within-tenant cache-first preserved (S2: WALLiam POST → King Shah, cache-first short-circuits RPC, verified via `[session] route hit` log without `[session] calling rpc`).

- **F-FLOOR-POOL-PERMISSION-DENIED (RESOLVED 2026-05-29, P-LIFECYCLE Landing 1 `4e69773`)** — Surfaced in EDIT C.5 sub-smoke 2 and re-confirmed during E2E S4: POST to `/api/walliam/assign-user-agent` with a `listing_id` whose cache is NULL returned HTTP 500 "permission denied for table `tenant_floor_pool`". Root cause (recon-confirmed in `phase-lifecycle-recon-output.txt`): two compounding issues. (1) `tenant_floor_pool` had grants ONLY for the postgres superuser - no SELECT for service_role; (2) the single RLS policy `tfp_read_own_tenant` filtered on `auth.uid()` which is NULL for any server-side caller without a tenant_users JWT, evaluating the IN-subquery to empty. The grant-layer check fires before RLS, so even adding a service_role grant wouldn't have unblocked the existing RLS path. **RESOLUTION:** P-LIFECYCLE Landing 1 ran `ALTER FUNCTION pick_floor_agent SECURITY DEFINER SET search_path = public, pg_temp`. Function body now runs with the owner's (postgres) privileges for its `tenant_floor_pool` SELECT, bypassing both the missing grant AND the auth.uid-keyed policy in one move. Surgical: only `pick_floor_agent` changed; resolver chain elsewhere remains SECURITY INVOKER. **Empirical fix-proof (V4 inside migration transaction):** `SET LOCAL ROLE service_role; SELECT pick_floor_agent(FLOOR_LISTING, WALLiam, true, false)` returned Neo Smith `f2ce3011-...`; pre-ALTER the same call raised the permission-denied error. V5 escalation through `resolve_agent_for_context` confirmed the end-to-end resolver chain works under service_role.

- **F-EVENT-4-ASYNC-PERMISSION-DENIED (RESOLVED 2026-05-30, P-LIFECYCLE Landing 3 Event 4 Step C `9255a18`)** — Surfaced during Event 4's async-handoff apply when post-COMMIT V3 attempted a `SET LOCAL ROLE service_role; UPDATE agents` and got `permission denied for table territory_reroll_queue`. Initially misread as a runner-only probe bug (queue SELECT under service_role with no grant); standalone probe revealed it was ALSO the actual production-path bug. `handle_agent_deactivate` was authored SECURITY INVOKER; `service_role` has zero grants on `territory_reroll_queue` (`information_schema.role_table_grants` shows postgres-only), so when the admin route (supabase-js -> PostgREST -> authenticator -> `SET LOCAL ROLE service_role` -> UPDATE agents) fires the trigger, the INSERT denies and the agent UPDATE fail-closed-rolls-back — same outcome as the sync version's 8s timeout, different error. **RESOLUTION:** one-line `ALTER FUNCTION public.handle_agent_deactivate() SECURITY DEFINER SET search_path = public, pg_temp` (class-mirror of Landing 1's `pick_floor_agent` flip). Function body unchanged; INVOKER->DEFINER safe because body has no `auth.uid()`/`current_user`/dynamic SQL, `NEW.tenant_id` is trigger-context-derived not caller-supplied, ON CONFLICT target is per-tenant. **Empirical fix-proof:** in-migration V2 + apply-runner post-COMMIT V2 + standalone T7 (King Shah twice + WALLiam seed once); all three PASSED with the LESSON pattern (queue reads under postgres, role-switch confined to the trigger-firing UPDATE).

- **F-CACHE-FIRST-IGNORES-ACTIVE (RESOLVED 2026-05-30, P-LIFECYCLE Landing 3 Event 4 Q2 hardening `9255a18`)** — Surfaced during Event 4 recon: the Phase 2 cache-first preamble (EDIT D `748ddd8`) added `agents.tenant_id` to the INNER JOIN but did NOT filter on `agents.is_active` or `agents.is_selling`. A stale cache row pointing at a deactivated agent would still route a new lead to them via cache-first (cache-hit short-circuits the RPC's tenant + active checks). **RESOLUTION:** the 7 cache-first reader sites (`lib/utils/tenant-resolver.ts`, `lib/actions/leads.ts`, and 5 API routes) now filter the cache-first INNER JOIN on `agents.is_active = true AND agents.is_selling = true` in addition to `agents.tenant_id`. Deactivated/not-selling agents become cache-miss and fall through to the RPC. This is the orthogonal-safety half of Event 4: it holds correctness REGARDLESS of reflow latency, so the cron's worst-case staleness window is masked. Pre-apply live probe confirmed zero current routing agents have `is_selling IS NULL` (schema-NOT-NULL on `is_selling` + default true) so no regression risk; all 10 agents are active+selling.

- **F-REROLL-WORKER-OPERATOR-PULL-ONLY (CLOSED 2026-05-30 by cron in `9255a18`)** — Pre-existing operational gap surfaced during Event 4 async-design recon: the `app/api/admin-homes/territory/reroll-worker/route.ts` worker was drained ONLY by browser polling from `TerritoryCascadeChart.tsx` (10s cadence while the cockpit chart is mounted). No cron, no GH-Action, no scheduled invocation. Evidence: one pending queue row from 2026-05-27 had sat undrained for 3 days at recon time. Event 4's async design depends on the worker draining; without autonomous invocation the queue piles up. **RESOLUTION:** new `.github/workflows/reroll-worker.yml` cron drains every 5 minutes across the configured tenant list, separate from `nightly-sync.yml` (F-NIGHTLY-SYNC-TIMEOUT-6H caution). Auth via the worker route's new fail-closed Bearer-token bypass.

- **F-EXISTING-HANDLE-APA-TRIGGERS-SAME-LATENT-RISK (RESOLVED 2026-05-30, F-apa-secdef-sweep `d2f0e69`)** — All three `handle_apa_*` AFTER-row trigger handlers (`handle_apa_insert`, `handle_apa_update`, `handle_apa_delete`) on `agent_property_access` were SECURITY INVOKER and INSERTed into `territory_reroll_queue` (postgres-only grants, zero for `service_role`). Latent because every production APA write path uses pg-direct (`bulk-create`, `bulk-restore`, `bulk-deactivate`, `bulk-reassign`, `cleanup`, `cards/route`, `bulk-assign` -- all 7 connect via `DATABASE_URL` as `postgres`). The first supabase-js -> service_role APA write would have hit permission-denied -- same shape as F-EVENT-4-ASYNC-PERMISSION-DENIED. **RESOLUTION:** three `ALTER FUNCTION ... SECURITY DEFINER SET search_path = public, pg_temp` (class-mirror of Landing 1's `pick_floor_agent` flip + Event 4 Step C's `handle_agent_deactivate` flip). Function bodies UNCHANGED. `apa_mutation_lock_trigger` stays INVOKER (body verified in recon: only `pg_advisory_xact_lock`, no table writes, no grants needed). **Verified under service_role** by V1-V6 in-tx (the migration's own SAVEPOINT-isolated asserts) + T1-T5 smoke harness: V2/V3/V4 prove each handler's queue INSERT succeeds under `service_role` (the async-path, the actual permission gap); **V5 (in-migration) + T4 (smoke)** prove the FULL SYNC chain incl. inner `PERFORM reroll_listings_at_geo` inherits postgres privileges through the DEFINER chain (inner-function inheritance proven empirically, not assumed). **Test-design bug caught + fixed mid-apply:** initial V3 used `UPDATE ... SET is_primary = TRUE` which is correctly a no-op (is_primary is NOT in `handle_apa_update`'s `v_routing_changed` predicate; only routing-field changes enqueue). Migration rolled back cleanly with no production state changed (atomic PG tx); V3/T2 fixed to `SET is_active = FALSE` (a TRUE->FALSE transition confirmed against setup state which inserts with `is_active=TRUE`); re-audited V2-V5/T1-T5 for the same class before re-apply. Re-applied successfully on second attempt; smoke T1-T5 PASSED. Snapshot of pre-flip handler definitions preserved at `supabase/migrations/rollback-snapshots/_f-apa-secdef-sweep_handlers_2026-05-30T20-17-56-548Z.sql` (forensic; down doesn't use it since bodies are unchanged).

- **F-SMOKE-CONNECTION-DROP-ON-LARGE-REFLOW (OPEN, low, harness bug)** — Surfaced during Event 4 smoke T6: a worker-drain end-to-end test on King Shah (441,066 listings) crashed mid-reflow with `Connection terminated unexpectedly` from the pg client; no `.on('error')` handler was attached so the Node process died. Most likely cause: Supavisor session pooler enforces a per-connection time limit beyond what `statement_timeout = 0` overrides. **Not a production issue** — the worker route attaches `.on('error')` handlers and is designed to handle drops gracefully; this is a smoke-harness gap. **Verified zero DB state left** after the crash via read-only probe: King Shah `is_active=true, is_selling=true`, no orphan `processing`/`pending` queue rows, exact 441,066 holdings restored, coupled-CHECK intact DB-wide (PostgreSQL's automatic rollback on connection loss covered for the harness bug). **Fix shape (deferred):** add `.on('error', ...)` handlers to the smoke harness pg clients; consider switching T6 to a smaller test agent (or skip T6 in non-manual runs) since the 441k-scale test is wall-clock-heavy. T7 (the actual production-path gate) is small and reliable.

- **F-CRON-TOKEN-NOT-CONSTANT-TIME (OPEN, low, defense-in-depth)** — The worker route's cron-bypass token check uses JavaScript `===` for the Bearer-header comparison, which short-circuits on first mismatched byte (not constant-time). Not exploitable over HTTPS with a 32-char random secret (TLS jitter swamps any per-byte timing signal), but the correct pattern for a standing token is `crypto.timingSafeEqual(Buffer.from(received), Buffer.from(expected))` after length-checking. Single 4-line swap; deferred to a cleanup commit.

- **F-QUEUE-DUPLICATE-INDEX (OPEN, low, cleanup)** — `territory_reroll_queue` has two identical UNIQUE indexes on `(tenant_id, scope, scope_id) WHERE status='pending'`: `uq_reroll_queue_pending_slot` (from `20260524_w_cockpit_p_b_2_c2a_reroll_queue.sql` migration) and `uq_trq_pending_dedup` (from an earlier related migration). Both correctly enforce the per-pending-slot dedup, so functionality is unaffected, but one is redundant storage + maintenance overhead. Drop one in a future cleanup commit; defer until convenient.

- **F-TENANT-FLOOR-ALERTS-SAME-GRANT-WALL (DOCUMENTED 2026-05-31, P-DASHBOARD CORE-5 recon)** — Third sibling table joining `tenant_floor_pool` (Landing 1's F-FLOOR-POOL-PERMISSION-DENIED) and `territory_reroll_queue` (Event 4's F-EVENT-4-ASYNC-PERMISSION-DENIED) in the postgres-only-grants class. All three: `service_role` has zero `information_schema.role_table_grants`, plus RLS=true with an `auth.uid()`-keyed policy that evaluates empty for server-side callers. **Operational rule**: any server-side read or write on these three tables MUST be pg-direct as `postgres`, NOT supabase-js -> PostgREST -> service_role. Surfaced via P-DASHBOARD CORE-5 build when the initial GAP-B implementation used supabase-js and silently soft-failed to `[]` (the false-green class -- see F-FALSE-GREEN-VIA-SILENT-SOFT-FAIL in PART 6). **Resolution**: P-DASHBOARD CORE-5's GAP-B + GAP-D fix already switched the health route's reads to pg-direct (`3a4167a`); the finding is documented here as the operational rule for any future code that touches these tables. A future migration that GRANT-SELECT-to-service-role on these tables + rewrites the broken auth.uid() RLS policies could close the class structurally, but is NOT urgent (every current path uses pg-direct successfully).

- **F-POSTGREST-COUNT-EXACT-SILENT-500 (OPEN, low, PostgREST-specific)** — Surfaced during P-DASHBOARD CORE-5 post-build smoke: supabase-js `.select('id', { count: 'exact', head: true }).is('assigned_agent_id', null)` against the 1.3M-row `mls_listings` table returns `status: 500, error: { message: "" }` (empty message body). Direct SQL `SELECT COUNT(*) FROM mls_listings WHERE assigned_agent_id IS NULL` under `SET LOCAL ROLE service_role` succeeds in <100ms returning the real count (3,7xx). The mismatch is PostgREST-specific: most likely the 8s authenticator `statement_timeout` cancels the count query and PostgREST surfaces the cancel as a 500 with no error body. **Operational rule**: route any large-table count via pg-direct as `postgres`, not via supabase-js `count: 'exact'` -- empty error body makes silent soft-fail likely (which then produces false-green per the next finding). Affects ANY supabase-js count over a multi-100k-row table where PostgREST may not pick the optimal index plan. Not blocking today; recorded for the next session that reaches for `count: 'exact'`.

- **F-ESTIMATOR-MIXES-RENT-INTO-SALE-COMPS (RESOLVED 2026-06-01 — P1 FIX 1/3)** — The estimator's comparable-matching SQL (in `lib/estimator/comparable-matcher-sales.ts` and `home-comparable-matcher-sales.ts`) filtered on `close_date IS NOT NULL` but did NOT filter on `transaction_type = 'For Sale'`. `close_date` is populated for both sold listings AND lease-end records, so rental records polluted the sale comparable set. First CV-EST run on a Whitby 2BR condo pulled 5 comparables with `close_price` ranging $2,500 (a rent termination) to $573,000 (a real sale), yielding a meaningless $116,755 estimate. The Sonnet 4 AI commentary actually caught the inconsistency in `keyFactors` ("Data inconsistency between rental and sale prices"). **Recon** (read-only probe `scripts/cv-comp-matcher-discriminator-probe.js`): confirmed `transaction_type` as the correct discriminator (302K `For Sale`, 291K `For Lease`, 76 `For Sub-Lease`, 0 NULLs in closed-with-date rows); identified 2 production leases above the existing `close_price > 100000` floor (W10407085 $581,500 condo, X10427465 $285,000 home), proving the floor was never the real discriminator. Confirmed scope as a CLASS bug across both sale-matcher variants (1 condo query + 2 home cascade queries = 3 lines, 2 files); confirmed both rentals matchers (`comparable-matcher-rentals.ts`, `home-comparable-matcher-rentals.ts`) already correctly filter `For Lease` → no mirror bug. **Fix:** added `.eq('transaction_type', 'For Sale')` to `comparable-matcher-sales.ts` (1 query) and `home-comparable-matcher-sales.ts` (community + muni cascades), leaving the existing `close_price > 100000` floor in place as belt-and-suspenders. **Smoke** (`scripts/smoke-fix-rent-comps.js`, 10/10 PASS): Building A (1606 Charles St, real 69-lease/3-sale 2BR pool) → post-patch query returns ZERO non-`For Sale` rows, estimate $657,667 with no rent contamination; Building B (712 Rossland Rd, CV-EST clean baseline) → estimate $561,000 unchanged (regression check); rentals path returns 5 leases unchanged ($2,450–$2,725/mo). tsc clean.

- **F-ESTIMATOR-SUBLEASE-NOT-MATCHED (OPEN, P3, design-decision, surfaced 2026-06-01 in P1 FIX 1 recon)** — `transaction_type` has three values in `mls_listings`: `For Sale` (302K), `For Lease` (291K), `For Sub-Lease` (76). The P1 FIX 1 patch added `.eq('transaction_type', 'For Sale')` to the sale matchers; the existing rentals matchers already filter `.eq('transaction_type', 'For Lease')`. Sub-lease rows (76 closed in last 24mo, p50 close_price $2,300/mo) therefore fall through BOTH matchers — they aren't returned in sale comps (correct) and aren't returned in rentals comps either (orphaned). Volume is tiny vs `For Lease` (~0.03%) and the user-facing effect is benign (rentals estimate is slightly less inclusive than it could be), so this is a design decision, not a bug: optional widening of the rentals matcher to `.in('transaction_type', ['For Lease', 'For Sub-Lease'])` in a separate landing. **Fix shape (deferred):** decide rentals-vs-orphan policy per product spec, then widen both rentals matchers in one commit; smoke against a building with sub-lease comps.

- **F-REROLL-LISTINGS-AT-GEO-COUPLED-CHECK (RESOLVED 2026-06-01 — P1 FIX 2/3)** — `public.reroll_listings_at_geo(scope, scope_id, tenant)` wrote `mls_listings.assigned_agent_id` ONLY, leaving `assigned_scope` / `assigned_source_id` untouched. Three half-NULL violation paths against `mls_listings_assigned_coupled_check`: (a) empty routing set → agent NULLed while scope stays non-NULL; (b) NULL-cache hit → agent set while scope stays NULL; (c) overwrites pin/building rows with a community/muni/area pick (constraint holds but scope is now a LIE). **Recon (read-only probe `scripts/cv-reroll-coupled-check-recon.js` → `cv-reroll-coupled-check-recon-output.txt`; synthesis `f-reroll-coupled-check-recon.md`):** the LIVE body did NOT match the on-disk migration `supabase/migrations/20260507_t3b_b_01_distribution_functions.sql` (set-based hash-RR live vs per-row cursor on disk) — applied-but-uncommitted, the v22-class git-drift scenario. Tracker's earlier "at-risk-row probe = 0" was stale: 2 prod rows (Commercial, municipality scope) were firing-capable today. Direct delegation to `reresolve_listings_in_set` rejected because its sticky guard (`assigned_scope < p_scope`) blocks re-pick at the SAME scope — wrong for reroll's force-re-pick semantic. **Fix (migration `20260601_f_reroll_coupled_check_fix.sql`, apply-runner + smoke harness committed alongside):** rewrote the body as 5-step NULL-trio-then-delegate — (1) collect listings in geo with `scope_specificity(assigned_scope) <= scope_specificity(p_scope)` (excludes pin/building → closes class c); (2) capture pre-state agents into jsonb; (3) atomic UPDATE: trio → NULL/NULL/NULL; (4) PERFORM `reresolve_listings_in_set(listing_ids, p_tenant_id)` — property_type split + floor-pool fallback inherited from the delegate, NOT re-implemented; (5) RETURN count of rows whose final agent != pre-state agent (preserves worker route's `rowsUpdated` metric). `ALTER FUNCTION ... SECURITY DEFINER SET search_path = public, pg_temp` (was INVOKER + no lock). Signature `(text, uuid, uuid) RETURNS integer` UNCHANGED — 3 trigger callers + 1 worker route untouched. `CREATE OR REPLACE` reconciles the git drift atomically. **Verification:** in-tx V1-V4 PASS (V4 is SAVEPOINT-isolated: empty-pool fixture on Whitby Industrial 4b3e61ae..., 13 community-scoped listings, sentinel-rollback) → COMMIT; post-COMMIT shape verify under postgres (no role-switch per v25) PASS. Smoke 5/5 PASS: T1 shape, **T2 pin on listing 51c485ea... survived community reroll** (sticky), **T3 zero half-NULL post empty-pool**, **T4 bit-identical (agent, scope, source) on all 13 listings between path A (new reroll) and path B (manual NULL+delegate) — walk-equivalence proven**, T5 return value `n=13 == observed diff=13`. Post-state confirmed: WALLiam community apa for Whitby Industrial still 1 active / 0 inactive (V4 + T3-T5 deactivations rolled back); 13 listings still at scope=community; coupled CHECK invariant holds across all 1,303,974 rows (0 agent_only, 0 scope_only). Pre-fix body snapshotted at `supabase/migrations/rollback-snapshots/_f-reroll-coupled-check_2026-06-01T12-18-46-292Z.sql`. Down-migration at `supabase/migrations/20260601_f_reroll_coupled_check_fix_down.sql` (restores live pre-fix body verbatim).

- **F-PLATFORM-MANAGER-TENANTS-SERVICE-ROLE-GRANT (OPEN, P1, latent, surfaced 2026-05-31 in CV-HIERARCHY)** — `service_role` lacks `SELECT` on `public.platform_manager_tenants`. The Layer-5 query inside `getLeadEmailRecipients` (`lib/admin-homes/lead-email-recipients.ts:208-213`) hits this when called from `createLead` (which uses `createServiceClient`). supabase-js silently returns `{ error }` for the failed PostgREST request; the code at L213 then falls through to `assignedAdminIds = []`, dropping Layer-5 BCC entirely. Currently invisible because no tenant has assigned manager platforms (count=0 across the DB), but any future `platform_manager_tenants` row would be effectively muted under `service_role`. Same class as `tenant_floor_pool` / `tenant_floor_alerts` / `territory_reroll_queue` grant findings already in this tracker. **Fix:** `GRANT SELECT ON public.platform_manager_tenants TO service_role` in a migration, OR flip the Layer-5 query to a SECURITY DEFINER wrapper that runs the SELECT as postgres.

- **F-EMAIL-PREFLIGHT-ACCEPTS-PLACEHOLDER-KEY (OPEN, P1, behavioral, surfaced 2026-06-01 in CV creds work)** — `sendTenantEmail.ts:71-79` pre-flight checks tenant credential fields with truthy tests: `if (!tenant.resend_api_key) missing.push(...)`. A literal placeholder string like `REPLACE_..._KEY` is truthy, so it passes pre-flight. The send then proceeds to `new Resend(tenant.resend_api_key!)` at L82 and fails at the Resend API call (auth-rejected), throwing `TenantEmailFailed` instead of `TenantEmailNotConfigured`. Same end-state (no email delivered) but wrong error class — and operationally surprising because pre-flight is supposed to catch unconfigured tenants. **Fix:** add a shape check to the pre-flight: `if (!tenant.resend_api_key || !tenant.resend_api_key.startsWith('re_')) missing.push('resend_api_key (invalid shape)')`. Same pattern for `tenant.anthropic_api_key` if any code reads it without shape validation. The real WALLiam key passes today; this is the hole that lets a future stale-revert (re-pasting `REPLACE_..._KEY`) slip past the gate again instead of throwing immediately.

- **GAP-7-DASHBOARD-UNDERSPECIFIED (CLOSED 2026-05-31 by P-DASHBOARD CORE-5 `3a4167a`)** — The dashboard spec ambiguity from v16 ("run distributions, view coverage, assignment-vs-default per geo, see floor alerts") was closed via the P-DASHBOARD recon (`p-dashboard-recon-output.txt`, 2026-05-30) which found that 4 of the 4 brief-mentioned views (Agents / Cards / Geography / Health) were ALREADY shipped in the existing 7-view cockpit at `/admin-homes/tenants/<id>`'s Territory tab. P-DASHBOARD was therefore ADJUST + EXTEND, not greenfield. 5 concrete gaps identified (GAP-A discoverability + GAP-B floor alerts + GAP-C cron observability + GAP-D NULL-cache + GAP-E agent footprint) were closed in CORE-5 commit `3a4167a`. Three sub-items remain deferred: GAP-G (lift carve-up control to top-level, nice-to-have), GAP-H (cross-tenant overview for platform admins, separate phase), GAP-I (Event 7 reconcile_corrections view, gated on Event 7 ship). GAP-F (delete orphan TerritoryCascadeChart.tsx) deferred to a separate cleanup commit, not bundled with CORE-5.

- **GAP-8-LAUNCH-READINESS-UNDEFINED** — "P-COMMIT done" is NOT "ready for first paid customer." See PART 7 launch-readiness checklist. Committed routing code is necessary but not sufficient. Do not conflate build-complete with launch-ready.

---

# PART 6 — PROCESS LESSONS (carry forward)

- **No placeholders in SQL string-literal position** — Postgres treats `'REPLACE_ME'` as a valid value; the UPDATE succeeds with garbage. Use parameterized queries, GUI for credentials, or syntactically-invalid sentinels outside quotes.
- **Never `SELECT *` on credential-bearing tables** (`tenants`, `agents`) — leaks secrets to output/logs. Explicit column allow-lists; fingerprint-only display of any secret.
- **Credential writes use the GUI** (Supabase Studio Table Editor), never chat-drafted SQL.
- **Leak response is proportionate, not rushed** — confirm scope, ask the user rotate-vs-accept, execute carefully with GUI, verify by fingerprint. Rushed remediation caused more damage than the leak.
- **PowerShell `Set-Content -Encoding UTF8` writes a BOM** — strip it when reading SQL in Node (`if (s.charCodeAt(0)===0xFEFF) s=s.slice(1)`), or write no-BOM via `[System.IO.File]::WriteAllText(path, content, [System.Text.UTF8Encoding]::new($false))`.
- **Probe geo table names before join-heavy recon** — the table is `treb_areas`, not `areas`; assuming crashed a script.
- **Large set-based UPDATEs on `mls_listings` exceed the 60s pool timeout** — disable `statement_timeout` for those sessions, and consider that 1.28M-row operations take real time; prefer scoped/batched work and the materialized model that avoids mass rewrites entirely.
- **Recon over memory** — prior tracker text is hypothesis until verified against the live DB. Probe before designing.
- **JSX:** raw `->` is invalid (TS1382); use `{'->'}`. Anchors must be ASCII; detect CRLF vs LF.
- **Client disconnect ≠ DB rollback (v17 lesson, 2026-05-28)** — a committed-server-side / failed-client-side transaction is why tracker memory and live DB diverged — the cold-start DB check is what caught it.
- **Credential leak via Grep output_mode (v18 lesson, 2026-05-28)** — Grepping `^DATABASE_URL=` with `output_mode=content` echoed the full DB password into the chat transcript despite an explicit "do not print" instruction. Safe pattern for credential checks: a Node script that reads, classifies, and prints ONLY a binary verdict (e.g. "direct"/"pooler"/"missing") — never the raw value. Never grep with content output on `.env.local` or any credentials file.
- **Verification asserts must test invariants, not data assumptions (v19 lesson, 2026-05-29)** — `ASSERT count > 0` on set X only works if the v16 model guarantees X is non-empty. Audit every assert with the question "what happens if the data shape shifts by 1%?". If the answer is "the assert false-fails", reformulate as a relational invariant (e.g. `filled = condo+home + filled-commercial`) or a conditional-vacuous structure (e.g. `IF target_set_non_empty THEN assert correctness ELSE pass vacuously`). Caught at V5a after Phase 1 attempt 2: King Shah's 11 community carves are exhaustive of Whitby muni, so the V5a target set is empty; the original `ASSERT v_n > 0` could never pass against today's data. Same audit class hit V4b (hardcoded count range), V5b (hardcoded =11), V5c (hardcoded =2) — all reformulated.
- **Post-COMMIT smoke baselines must distinguish intended-change vs unintended-change (v19 lesson, 2026-05-29)** — comparing pre-migration state to post-migration state and flagging any difference as "regression" produces noise when the migration is meant to produce different state. Smoke needs migration-intent awareness: the baseline must capture which rows are EXPECTED to change and assert the new value matches resolver output (GAP-2), not just "differs from baseline". Caught at Phase 1 attempt 4: 63 false-positive carve-sample regressions (58 expected v15→v16 agent changes + 5 muni-carve baseline samples that the capture logic incorrectly expected to stay at scope='municipality' when they're inside community carves).
- **Session pooler vs transaction pooler (v19 lesson, 2026-05-29)** — Supabase port 5432 (session pooler) allocates one server connection per client session for the session's duration, so `SET LOCAL statement_timeout` persists across statements; port 6543 (transaction pooler) resets server state between statements and breaks `SET LOCAL`. The "pooler" substring in the hostname alone is not the discriminator — port is. Caught at Phase 1 attempt 3: this Supabase project does not expose a direct-host endpoint at all (`db.<projref>.supabase.co` is not in DNS — newer Supabase projects are pooler-only), so the apply-runner's original "reject any pooler" heuristic locked us out. Runner's check relaxed to reject port 6543 only.
- **Two-phase constraints when the migration populates the data (v19 lesson, 2026-05-29)** — `ADD CONSTRAINT … CHECK (…)` validates the constraint against pre-existing data at constraint-creation time. When the migration itself populates the data that satisfies the constraint, use `ADD CONSTRAINT … NOT VALID` (enforces the constraint on every subsequent write but skips back-validation) followed by `ALTER TABLE … VALIDATE CONSTRAINT` AFTER the population step. Caught at Phase 1 attempt 1: the coupled `mls_listings_assigned_coupled_check` ((agent NULL ⇔ scope NULL)) validated against 1.29M existing rows whose newly-added `assigned_scope` column was NULL while `assigned_agent_id` was non-NULL — instant failure before any population statement ran.
- **Cache-first abstractions must preserve the security filters of the path they bypass (v20 lesson, 2026-05-29)** — Phase 2's initial cache reads keyed only on `listing_id` reintroduced the tenant-leak class that `resolve_agent_for_context.p_tenant_id` was designed to prevent. When wiring a fast path that skips an existing secure path, the new path must enforce every filter the old path enforced — auth, tenant scope, RLS. Bypassed filters are bypassed protections. Caught at E2E S3 when an aily POST on a WALLiam-cached listing returned a WALLiam agent through the cache-first shortcut; closed in EDIT D by adding INNER JOIN on `agents.tenant_id` filtered by caller's tenant. Generalizable check before shipping any cache/index/denormalization that bypasses an RPC or view: list every filter that path enforced (auth, tenant, RLS, soft-delete, active-flag) and confirm the fast path enforces each one — or document why each is structurally unnecessary on the fast path.
- **Vacuous assertions hide real bugs (v20 lesson, 2026-05-29)** — Phase 1's V6 cross-tenant isolation ASSERT was structurally correct but vacuous because aily had no routing data at the time it ran. It passed because nothing could fail it, not because the system was correct. F-PHASE2-CACHE-NOT-TENANT-SCOPED was only catchable by actually attempting a cross-tenant POST in Phase 2 E2E. Assertions whose failure mode requires non-empty data must be exercised with that data, not just declared. For every "isolation" or "scope" ASSERT, add a live smoke that posts cross-boundary traffic — synthetic-but-realistic if production has no traffic on one side of the boundary. A passing vacuous ASSERT is not evidence the system is correct.
- **E2E smoke must verify the WHOLE pipeline, not just the surface (v20 lesson, 2026-05-29)** — Phase 2 E2E S3 only failed because the lead row was inspected post-creation, not because the route returned a wrong value (the route returned HTTP 200 with a leadId — looked fine). A smoke checking only the API response would have missed the leak entirely. Test the full pipeline: resolve → stamp → email recipient → audit trail. Surface-only smokes (HTTP status + return body) are not E2E. The standing rule: for every smoke that exercises a write-path, query the resulting persisted state and assert against expected tenant/owner/scope, not just against API-level success.
- **SECURITY DEFINER as a surgical grant-layer fix when the caller chain already enforces the relevant input filters (v21 lesson, 2026-05-29)** — F-FLOOR-POOL-PERMISSION-DENIED was a grant-layer block: `tenant_floor_pool` had grants only for postgres, and the RLS policy keyed on `auth.uid()` evaluated to empty for server-side callers. Two viable fix shapes were on the table: (a) GRANT SELECT to service_role + reframe the RLS policy, or (b) `ALTER FUNCTION pick_floor_agent SECURITY DEFINER` so its body runs with owner privileges. (b) was chosen as surgical: only the one helper function changes; resolver chain elsewhere stays INVOKER; no policy rewrite. The cost is that SECURITY DEFINER moves security-of-the-input from the DB layer to the app layer: the function trusts its `p_tenant_id` parameter unconditionally, so every caller MUST source `p_tenant_id` from a validated request context (header, tenant-scoped body field, middleware-derived) and never from raw user input. Document the contract on the function (via COMMENT) and audit every caller before applying SECURITY DEFINER. Generalizable rule: SECURITY DEFINER is appropriate when (1) the function body has no internal auth-dependent logic (no `auth.uid()`, no `current_user`), (2) every caller is auditable, and (3) the caller chain already validates the function's inputs at a higher layer. Lock `search_path = public, pg_temp` to prevent search-path-injection attacks on the elevated body. Recon (`phase-lifecycle-landing-1-precondition.txt`) verified all three for Landing 1; (1) was bytes of `prosrc` grep, (2) was the Phase 2 audit + a code-side grep showing zero direct callers, (3) was Phase 2 EDIT D's caller contract.
- **Set-based over per-row when the inner walk is already set-based primitives (v22 lesson, 2026-05-29)** — Landing 2's planning recon initially sketched a per-row `reresolve_listing_v2` + bulk wrapper called from the sync, ~200ms median per row -> 13-33 min added to nightly burst. Verify recon revealed (a) nightly sync ALREADY cancels at GH Actions' 6h cap every night so any added latency compounds the problem, and (b) `distribute_listings_at_geo` + `reroll_listings_at_floor` are already set-based and the provenance trio holds across 1.29M rows with zero exceptions. Re-shape: set-based primitive `reresolve_listings_in_set(uuid[], uuid)` does 10 CTE UPDATEs narrowed by `ml.id = ANY($1)`, wall-time scales with batch size + 5 round-trips (~30s for 10K rows, not 33min). Generalizable rule: before reaching for a per-row loop in TypeScript, check whether the equivalent already exists as set-based SQL primitives and whether your call can be expressed as one UPDATE per scope rather than N UPDATEs per row. Hash-RR over many buckets in one call is the SQL-native idiom — use it.
- **PARTITION BY for multi-bucket hash-RR (v22 lesson, 2026-05-29)** — `distribute_listings_at_geo` accepts a single `(scope, scope_id)` and computes hash-RR ranks over that ONE bucket via `ROW_NUMBER() OVER (ORDER BY apa.id)`. Landing 2's `reresolve_listings_in_set` runs N>1 communities/munis/areas in one call; the equivalent multi-bucket form is `ROW_NUMBER() OVER (PARTITION BY apa.<geo>_id ORDER BY apa.id) - 1 AS rn` + `COUNT(*) OVER (PARTITION BY apa.<geo>_id) AS total`. Per-partition ranks are bit-identical to per-bucket-called ranks; hash key (`abs(hashtext(ml.id::text)) % e.total`) and ordering (`apa.id`) match `distribute`'s exactly, so picks are byte-identical to what `distribute` would have produced if called per-bucket. Verify the equivalence at audit time, not at apply time.
- **Belt-and-suspenders tenant scoping: anchor + agents.tenant_id at every cascade level (v22 lesson, 2026-05-29)** — Phase 1's `distribute_listings_at_geo` and `reroll_listings_at_floor` scope by `apa.tenant_id` / `tfp.tenant_id` (the "anchor" tenant predicate) but do NOT additionally check `agents.tenant_id = p_tenant_id` on the joined agents row. The invariant "apa.tenant_id = T implies apa.agent_id belongs to T's agents" is operationally enforced today but NOT constraint-enforced at the schema level; a misconfigured apa or tfp row pointing to another tenant's agent would silently leak across tenants — the same class of bug that bit Phase 2 EDIT D. Landing 2 raises the bar: BOTH the anchor predicate AND `a.tenant_id = p_tenant_id` at every one of L1-L10. Cost: 10 extra `AND a.tenant_id = ...` clauses; benefit: structurally impossible for a misconfigured-anchor row to cross tenants through this function. Generalizable rule: at every step that touches a tenant-scoped table, scope BOTH the table you read from AND every joined table that has a `tenant_id` column. Audit checklist: grep for every JOIN on `agents`, `tenants`, or other tenant-bearing table inside the function body and require an explicit `<alias>.tenant_id = p_tenant_id` predicate next to it.
- **Predicate-based NULL safety beats early-guard short-circuit (v22 lesson, 2026-05-29)** — Landing 2's first draft had `IF p_listing_ids IS NULL OR cardinality = 0 OR p_tenant_id IS NULL THEN RETURN (0,0)` as an early guard. Audit caught that this MASKS the tenant-scoping correctness: if a future refactor accidentally removes a tenant predicate from one cascade level, the test that proves "NULL tenant returns (0,0)" still passes via the guard while the cascade silently misbehaves on real NULL-tenant calls. Fix: keep input-shape guards (`NULL listing_ids array` / `cardinality 0`) but REMOVE the `p_tenant_id IS NULL` clause; let NULL tenant be filtered by the cascade predicates themselves (`tenant_id = NULL` -> unknown -> zero rows match). V5 + smoke T4 now prove the no-op via predicate, not via guard — failure of any tenant predicate would visibly leak. Generalizable rule: never short-circuit on a value the cascade should already handle correctly via its WHERE clauses; an early guard that hides a latent bug is worse than a slightly slower function.
- **Commit applied migrations in the same session that applies them (v22 lesson, 2026-05-29)** — v15's P-FLOOR migration and v19's Phase 1 migration were both APPLIED to production but their `.sql` files + apply-runners + rollback snapshots were never committed to git. v19's tracker entry explicitly noted "Migration .sql files and apply-runner remain untracked in git" — a known drift, deferred, then forgotten. Landing 2 committed all artifacts in the same flow (the apply-runner + migration + smoke + down-runner + plan doc were the apply-then-commit pair), AND v22 backfilled v15/v19's drift as a separate commit (`4a9059e`, 12 files). Generalizable rule: if you applied a migration to production, the same session commits the migration `.sql` + apply-runner + rollback snapshot + down-migration to git BEFORE the session ends. Tracker's "Migration X.sql is untracked" note is a code smell; close it the same day. git history is the post-hoc audit trail and divergence from production schema is technical debt that compounds (next session's cold-start check reads tracker memory, not git).
- **`.claude/settings.local.json` is per-machine, not team-shared (v22 lesson, 2026-05-29)** — Claude Code's per-project config lives in `.claude/`. The `settings.local.json` variant is conventionally per-machine (operator-specific tool-permission allow/ask rules). It should NOT be tracked in git. CLAUDE.md (the standing-instructions file at repo root) IS team-shared and MUST be tracked. v22 added `.claude/` to `.gitignore` and committed `CLAUDE.md` (`dea6d71`). If a team-shared `.claude/settings.json` (no `.local`) ever arrives, narrow the gitignore to `.claude/settings.local.json` at that point. Generalizable rule: any tool-config file whose name ends in `.local.json` or `.local.<ext>` is per-machine; gitignore it. The non-`.local` sibling, if it exists, is team-shared and tracked.
- **SECURITY DEFINER for any trigger / function reachable through the admin route's service_role calling chain (v25 lesson, 2026-05-30)** — Functions that perform writes to grant-restricted tables MUST run as SECURITY DEFINER (with locked `search_path = public, pg_temp`) if any caller chain reaches them under `SET LOCAL ROLE service_role`. The Supabase admin route shape (supabase-js -> PostgREST -> `authenticator` -> `SET LOCAL ROLE service_role`) is exactly that chain; `service_role` has minimal direct table grants (e.g. zero grants on `territory_reroll_queue` — postgres-owner-only). An INVOKER trigger that INSERTs into such a table will fail permission-denied and roll back the calling UPDATE. Same shape as F-FLOOR-POOL-PERMISSION-DENIED (Landing 1) and F-EVENT-4-ASYNC-PERMISSION-DENIED (Landing 3 Event 4). Audit before applying any new trigger function: list every JOIN/INSERT/UPDATE target, check `information_schema.role_table_grants` for `service_role`, and either flip to DEFINER (when safe per the Landing 1 v21 lesson) or add explicit `GRANT INSERT/SELECT to service_role`. The DEFINER flip is safe when the body has no `auth.uid()`/`current_user`/`session_user`/`current_setting`/dynamic SQL; the tenant scoping is derived from trigger context (`NEW.<tenant_field>`) not caller input; and the function is auditable as a finite set of effects.
- **Test new triggers + functions under the ACTUAL failing role, not just postgres (v25 lesson, 2026-05-30)** — Event 4's first apply-runner ran in-tx V3 entirely as `postgres` (the migration runner's default role) and PASSED, then post-COMMIT V3 attempted `SET LOCAL ROLE service_role` and FAILED with `permission denied for table`. The bug-class-catcher is "does the thing work under the role that production callers actually use," not "does it work for the role that has all permissions." Pattern: every migration shipping a trigger or DEFINER function that will be reached from supabase-js -> PostgREST must include a V-assert that (a) `SET LOCAL ROLE service_role`, (b) exercises the actual production-path mutation (e.g. UPDATE agents to fire the trigger), (c) verifies the side effect, (d) ROLLBACK via SAVEPOINT-sentinel. Without this V-assert, a service_role-permission gap surfaces only AFTER COMMIT — and the COMMITted-broken state is worse than a rolled-back known-bad state. Reflected as in-migration V2 + apply-runner post-COMMIT V2 + smoke T7 in the Event 4 secdef-fix package.
- **Post-COMMIT apply-runner probes must NOT query restricted tables under role-switched windows (v25 lesson, 2026-05-30) — 2nd incident** — Landing 2's V3 (tenant pick under service_role) and Landing 3 Event 4 Step A+B's V3 (queue SELECT under service_role) both had the same shape: a `SET LOCAL ROLE service_role` followed by a SELECT on a table that service_role can't read. The probe fails for the WRONG reason and can mask the REAL production-path bug. The corrected pattern (LESSON, baked into the secdef-fix runner): (1) pre-state read under `postgres` (the default tx role), (2) `SET LOCAL ROLE service_role`, (3) execute ONLY the production-path mutation that fires the trigger, (4) `RESET ROLE`, (5) post-state read under `postgres`, (6) assert + ROLLBACK. The role switch is confined to the single statement that exercises the production path; all reads happen under the postgres role. Second time this class has bit us — Landing 2 was the first, Event 4 was the second; future apply-runners must follow this pattern by default.
- **Smoke pg clients need `.on('error')` handlers + smaller test fixtures for wall-clock-heavy paths (v25 lesson, 2026-05-30)** — Event 4 smoke T6 (worker drain on 441,066 listings) crashed with `Connection terminated unexpectedly` because no `.on('error')` handler was attached to the pg `Client` instance, so the unhandled error event killed the Node process. PostgreSQL's auto-rollback covered for the lost transaction, but the test result was indeterminate. Two rules: (a) every pg `Client` opened in a smoke harness must attach `c.on('error', e => console.error('CLIENT ERROR: ' + e.message))` so a connection drop becomes a captured failure not a process crash; (b) smoke tests that operate on a wall-clock-heavy fixture (>100k rows, multi-minute reflows) are at higher risk of pooler-side connection drops than the production worker (which uses pg-direct and is designed for this); pick a smaller test agent or split the heavy test into a manual-only path so CI smokes stay reliable.
- **False-green via silent soft-fail in health-signal UI (v27 lesson, 2026-05-31)** — UI-layer cousin of the v20 "vacuous assertions hide real bugs" lesson. P-DASHBOARD CORE-5's first GAP-B + GAP-D implementation soft-failed query errors to `floor_alerts: []` and `null_cache_count: { total: 0, ... }`. The HealthView render rendered these as healthy gray "0" panels -- visually identical to the genuine-empty healthy state. The diagnostics-warnings panel below was too subtle to overcome the headline number. Operator observed "0" and assumed healthy; in reality two reads had silently permission-denied (tenant_floor_alerts) and PostgREST-500'd (mls_listings count). **Rule**: any health-signal or dashboard read that can fail at runtime MUST distinguish "could not read" from "confirmed empty" at BOTH the data layer (e.g. return `null` on failure, NOT `[]` or `0`) AND the UI layer (e.g. render a red "could not read" panel, NOT a green check + 0). A healthy zero must not be representable by the same pixels as a failed read. Generalizable: every soft-fail path in monitoring/diagnostic surfaces is a candidate for false-green; audit each one for distinguishability before shipping. Audit checklist: for every `try { fetch X } catch { default to empty }` pattern, ask "if this catch fires, will the operator know?" If the answer is no, the catch is masking the bug it's catching.

---

# PART 7 — LAUNCH-READINESS CHECKLIST (WALLiam first paid customer)

The routing build is one input to launch, not launch itself. WALLiam is ready for its first paid customer only when ALL of these are true and verified (not assumed):

- [x] Routing build complete: Phase 1 + Phase 2 closed 2026-05-29. (Phase 1: P-ROUTING-SET + P-MATERIALIZE + P-REVERT-BACKFILL bundled atomic; cache verified v16-correct. Phase 2: cache-first reader wiring across 7 files, tenant-scoped via EDIT D `748ddd8`. P-LIFECYCLE remaining: resolve-at-insert + scoped re-materialization + reconcile.)
- [x] **F-WALLIAM-CREDS-PLACEHOLDER-IN-DB resolved for WALLiam (2026-06-01, v5 reopen `4cf4eed`)** — `tenants.anthropic_api_key` (fp `sk-ant...zwAA`) + `tenants.resend_api_key` (fp `re_BJJ...cqSr`) hold real keys, verified live (Resend `GET /domains` 200 + Anthropic `GET /v1/models` 200 + real send msg id `0a05149f-...` + real $561K AI valuation). Per-agent AI flag + key wired on all 3 WALLiam agents. **Aily caveat:** Aily has real keys in its tenant cols too, BUT `resend_verification_status=NULL` and `send_from`/`email_from_domain` still point at `condoleads.ca` → Aily sends will fail pre-flight until operator verifies an Aily-owned domain in Resend + updates the identity columns. Aily-launch path is BLOCKED-ON-DOMAIN-VERIFICATION; WALLiam-launch path is clear.
- [x] **Dashboard built (after its design pass — GAP-7) so the operator can actually run distributions and see coverage.** GAP-7 design pass CLOSED 2026-05-31 by P-DASHBOARD CORE-5 `3a4167a` (per its PART 5 entry: P-DASHBOARD recon confirmed 4 of 4 brief-mentioned views — Agents / Cards / Geography / Health — were ALREADY shipped in the existing 7-view cockpit at `/admin-homes/tenants/<id>`'s Territory tab; CORE-5 was ADJUST + EXTEND). CORE-5's 5 enhancement gaps (discoverability redirect, agent footprint + reflow-preview, floor-pool alerts, cron observability, NULL-cache visibility) all shipped. The "operator can run distributions + see coverage" capability is satisfied by the pre-existing cockpit; CORE-5 added the operator-friendliness layer on top. Three sub-items deferred (GAP-G top-level carve-up control, GAP-H cross-tenant platform-admin overview, GAP-I Event-7 reconcile-corrections view) — none block the launch capability test.
- [x] **End-to-end lead flow verified live (2026-06-01 via W-CORE-VERIFICATION).** CV-LEADS proved all 7 lead-writing mechanisms submit → resolve (cache-first + RPC) → stamp `leads.agent_id` + full chain (manager/area_manager/tenant_admin) → invoke envelope (`getLeadEmailRecipients` correctly fans out TO/CC/BCC up the chain) end-to-end, under postgres AND service_role. CV-CHARLIE proved appointment booking + lead capture against a carved Whitby community (Brooklin → King Shah). CV-EMAIL + CV-EST proved real Resend sends to `delivered@resend.dev` (msg ids `0a05149f-...` and `70bdb36d-...`); CV-EST also proved real $561K Sonnet 4 valuation on a Whitby BINGO-tier listing. CV-CROSS proved the floor-pool path (and same-Markham collision: 0 leaks across 64 isolation probes). **Caveat: verified via real-API smoke harness (`scripts/smoke-cv-*.js`), not a literal browser submission.** Workbench appearance is the only sub-step not directly asserted — the lead INSERT writes the row the workbench reads, but no UI render check was made. (Browser-render smoke is in scope of GAP-6 / future operator-UX validation.)
- [ ] Materialized read path scale-tested (GAP-6) — geo page render time acceptable.
- [ ] Reconcile job running and its corrections-log near zero (proves triggers are catching everything).
- [ ] Paddle KYC complete (EXTERNAL dependency — per project memory, running in parallel; not a code task but a launch gate).
- [ ] **No P0/P1 open findings in PART 5.** One open P1 as of 2026-06-01 (down from three after P1 FIX 1/3 + P1 FIX 2/3; defer-able after WALLiam soft-launch but should not ship to multi-tenant scale):
    - **F-PLATFORM-MANAGER-TENANTS-SERVICE-ROLE-GRANT** — latent; silently drops Layer-5 BCC; currently invisible (no platform_manager_tenants rows DB-wide). Internal-only.
    - (F-EMAIL-PREFLIGHT-ACCEPTS-PLACEHOLDER-KEY is P1 behavioral but only fires on stale-revert of the placeholder; the real keys pass today so it's a defense-in-depth fix, not a release blocker.)
    - (F-ESTIMATOR-MIXES-RENT-INTO-SALE-COMPS — RESOLVED 2026-06-01 by P1 FIX 1/3; see PART 5 entry.)
    - (F-REROLL-LISTINGS-AT-GEO-COUPLED-CHECK — RESOLVED 2026-06-01 by P1 FIX 2/3; see PART 5 entry.)

Build-complete (P-COMMIT) ticks the first box only. The rest are real launch gates.