W-ESTIMATOR-HIERARCHY-CONFORMANCE — RECON (read-only)
Date: 2026-06-17
Method: file reads + grep on the live origin/main tree. No DB writes,
        no commits.

═══════════════════════════════════════════════════════════════════════════
ONE-LINE VERDICT
═══════════════════════════════════════════════════════════════════════════

  The estimator path CONFORMS to the locked Lead+Email contract.

  It calls the canonical getLeadEmailRecipients helper, walks the full
  hierarchy chain via walkHierarchy, stamps all five hierarchy fields
  on the leads row, and fans email out via TO/CC/BCC. Code-wise this
  is byte-identical to Charlie's working path. The "estimator still
  failing post-deploy" symptom is NOT a hierarchy-conformance divergence.

═══════════════════════════════════════════════════════════════════════════
R1 — DOES THE ESTIMATOR PATH CALL THE RECIPIENTS HELPER?
═══════════════════════════════════════════════════════════════════════════

  ESTIMATOR call site:
    lib/actions/leads.ts (the path submitLeadFromForm delegates to):

      L19:  import { walkHierarchy } from '@/lib/admin-homes/hierarchy'
      L24-26: import {
                getLeadEmailRecipients,
                ...
              } from '@/lib/admin-homes/lead-email-recipients'

      L247:  recipients = await getLeadEmailRecipients(
                params.tenantId,
                resolvedAgentId,
                supabase
             )

      L308-316:  await sendTenantEmail({
                   tenantId: params.tenantId,
                   to:  recipients.to,
                   cc:  recipients.cc.length > 0 ? recipients.cc : undefined,
                   bcc: recipients.bcc.length > 0 ? recipients.bcc : undefined,
                   subject,
                   html,
                 })

  CHARLIE call site (the working comparison path):
    app/api/charlie/plan-email/route.ts:

      L17:  import { walkHierarchy } from '@/lib/admin-homes/hierarchy'
      L19-21: import {
                getLeadEmailRecipients,
                ...
              } from '@/lib/admin-homes/lead-email-recipients'

      L374:  recipients = await getLeadEmailRecipients(
                tenantId || '',
                agent?.id || null,
                supabase
             )

      Then attemptTenantEmail fans out via recipients.to / cc / bcc
      (L387-407, the chain notification block).

  RESULT: BYTE-IDENTICAL helper usage. Same import surface
  (@/lib/admin-homes/lead-email-recipients), same call signature
  (tenantId, agentId, supabase), same TO/CC/BCC fan-out shape.

  HELPER CONTRACT (lib/admin-homes/lead-email-recipients.ts:5-10):
      Layer 1: assigned agent              -> TO
      Layer 2: manager (walker)            -> CC
      Layer 3: area_manager (walker)       -> BCC
      Layer 4: tenant_admin (walker)       -> BCC
      Layer 5: Manager Platform (per tenant assignment) -> BCC
      Layer 6: Admin Platform (UNCONDITIONAL) -> BCC

  The estimator's getLeadEmailRecipients call returns this same
  6-layer set; the sendTenantEmail call at lib/actions/leads.ts:308-316
  delivers to all 6 via TO/CC/BCC.

  ▶ R1 VERDICT: estimator CONFORMS. The helper is called from
    lib/actions/leads.ts:247 (the path submitLeadFromForm → createLead
    reaches). No route-level branching around the helper. The
    estimator is NOT sending a narrower agent-only set; it sends to
    the full 6-layer chain the helper resolves.

═══════════════════════════════════════════════════════════════════════════
R2 — DOES THE ESTIMATOR STAMP THE FULL CHAIN ON THE LEAD ROW?
═══════════════════════════════════════════════════════════════════════════

  ESTIMATOR insert (lib/actions/leads.ts):

      L185-190: walker resolves the chain
        if (resolvedAgentId) {
          const chain = await walkHierarchy(resolvedAgentId, supabase)
          chainManagerId      = chain.manager_id
          chainAreaManagerId  = chain.area_manager_id
          chainTenantAdminId  = chain.tenant_admin_id
        }

      L207-231: insert payload
        await supabase.from('leads').insert({
          tenant_id:        params.tenantId,
          agent_id:         resolvedAgentId,
          manager_id:       chainManagerId,
          area_manager_id:  chainAreaManagerId,
          tenant_admin_id:  chainTenantAdminId,
          building_id:      params.buildingId || null,
          listing_id:       params.listingId || null,
          source_url:       params.sourceUrl || referer || null,
          estimated_value_min: params.estimatedValueMin || null,
          estimated_value_max: params.estimatedValueMax || null,
          property_details: params.propertyDetails || null,
          contact_name:     params.contactName,
          contact_email:    params.contactEmail,
          contact_phone:    params.contactPhone,
          message:          params.message,
          source:           source,
          lead_origin_route: deriveLeadOriginRoute(source),
          assignment_source: resolvedAgentId ? 'geo' : 'admin',
          status:           'new',
          created_at:       new Date().toISOString()
        })

  All FIVE locked-contract hierarchy fields are stamped:
    tenant_id        ✓ (L211, from header-resolved tenantId)
    agent_id         ✓ (L212, from resolveAgentForLead RPC)
    manager_id       ✓ (L213, from walkHierarchy)
    area_manager_id  ✓ (L214, from walkHierarchy)
    tenant_admin_id  ✓ (L215, from walkHierarchy)

  CHARLIE insert (app/api/charlie/plan-email/route.ts):

      L280-283: identical walker
      L341-343: stamps manager_id + area_manager_id + tenant_admin_id
        manager_id:       chainManagerId,
        area_manager_id:  chainAreaManagerId,
        tenant_admin_id:  chainTenantAdminId,

  Charlie uses its own inline supabase.from('leads').insert at L294
  (separate from lib/actions/leads.ts), but stamps the SAME five
  hierarchy fields via the SAME walkHierarchy resolver.

  HOW THE ESTIMATOR REACHES THIS INSERT:
    Client (EstimatorResults.tsx:253 / HomeEstimatorResults.tsx)
      → submitLeadFromForm({ forceNew: true, ... })
      → getOrCreateLead({ forceNew: true, ... }) — L122-163
      → forceNew: true branch at L130-133 → createLead(params)
      → createLead at L165 stamps the full chain + calls helper

    The forceNew: true flag means the estimator path BYPASSES the dedup
    check (L141-159) and ALWAYS goes through createLead → full chain
    stamping + helper fan-out. No degraded path.

  ▶ R2 VERDICT: estimator stamps the FULL chain. No NULL hierarchy
    layers on the insert. Equivalent to Charlie's stamping.

═══════════════════════════════════════════════════════════════════════════
R3 — IS THE ESTIMATOR CONNECTED TO THE SAME FAN-OUT AS WORKING ROUTES?
═══════════════════════════════════════════════════════════════════════════

  Compare the lead+email pipelines:

    ┌──────────────────────────────────────────────────────────────────┐
    │ ESTIMATOR (when client submit reaches the server action)         │
    │                                                                  │
    │  submitLeadFromForm                                              │
    │    ├─ headers().get('x-tenant-id')                               │
    │    └─→ getOrCreateLead({forceNew:true, ...})                     │
    │        └─→ createLead(params)                                    │
    │            ├─ resolveAgentForLead RPC          → resolvedAgentId │
    │            ├─ walkHierarchy(resolvedAgentId)   → chain           │
    │            ├─ leads.insert {tenant_id, agent_id, manager_id,     │
    │            │                area_manager_id, tenant_admin_id…}  │
    │            ├─ getLeadEmailRecipients(           → recipients     │
    │            │     params.tenantId, resolvedAgentId, supabase)     │
    │            ├─ sendTenantEmail({to: recipients.to,                │
    │            │                   cc: recipients.cc,                │
    │            │                   bcc: recipients.bcc, ...})        │
    │            └─ buyer-email send (P-WORKING-DOC) for buyer copy    │
    └──────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────────────┐
    │ CHARLIE plan-email (the working route)                           │
    │                                                                  │
    │  POST /api/charlie/plan-email                                    │
    │    ├─ middleware injects x-tenant-id (since /api/*)              │
    │    ├─ session-row read → tenant_id (DB fallback)                 │
    │    ├─ walkHierarchy(agent.id) → chain                            │
    │    ├─ supabase.from('leads').insert {tenant_id, agent_id,        │
    │    │     manager_id, area_manager_id, tenant_admin_id …}         │
    │    ├─ getLeadEmailRecipients(tenantId, agent.id, supabase)       │
    │    └─ attemptTenantEmail(... recipients.to/cc/bcc ...)           │
    └──────────────────────────────────────────────────────────────────┘

  STRUCTURAL DIFFERENCES (NONE substantive for the contract):
    • Estimator routes through the shared `lib/actions/leads.ts` helper
      module (used by 8 client callers).
    • Charlie does an inline insert in its own route.ts (a deliberate
      separation per the file comment header in lib/actions/leads.ts).
    • Both paths derive tenant_id, both call walkHierarchy, both stamp
      the same five hierarchy fields, both call getLeadEmailRecipients,
      both send via TO/CC/BCC. The contract surface is identical.

  No "estimator-only" branch suppresses the helper or short-circuits
  to agent-only email. The estimator is wired to the same 6-layer
  fan-out the rest of the system uses.

  ▶ R3 VERDICT: estimator IS connected to the same helper-based
    fan-out as Charlie + the other contract-conformant routes. No
    divergence at the wiring level.

═══════════════════════════════════════════════════════════════════════════
R4 — INTERACTION WITH THE x-tenant-id FIX (e79c670)
═══════════════════════════════════════════════════════════════════════════

  Pre-e79c670:
    Server-action POSTs (which the estimator's submitLeadFromForm
    is) never received x-tenant-id from middleware (the /api/*-only
    injection gate). submitLeadFromForm:51-58 read null, returned
    {success:false, error:'Tenant context unavailable.'} BEFORE
    reaching getOrCreateLead. The hierarchy fan-out at L185-316 was
    NEVER REACHED on any estimator submit. Lead-write + email fan-
    out were both dead — but not because of contract divergence; they
    were dead because the path early-exited at the tenant null check.

  Post-e79c670:
    Server-action POSTs now receive x-tenant-id (proven live by
    joinTenant succeeding at 2026-06-17 09:47:32; see recon/
    estimator-postdeploy-failure.txt R2). submitLeadFromForm:51-58
    passes the null check. getOrCreateLead → createLead executes.
    createLead walks the chain, stamps all 5 fields, calls the
    helper, fans email.

  → e79c670 is NECESSARY AND SUFFICIENT at the hierarchy/email-
    fan-out layer. There is no additional wiring missing. The
    estimator's lead-write + chain fan-out have always been
    correctly wired in the source; they just couldn't be reached
    before the header was propagated.

  ▶ R4 VERDICT: header fix is sufficient AT THE HIERARCHY LAYER.
    The hierarchy fan-out is intact and conformant.

═══════════════════════════════════════════════════════════════════════════
ROOT CAUSE ANALYSIS — re-anchored against operator's symptom
═══════════════════════════════════════════════════════════════════════════

  Operator's lingering symptom (post-e79c670): "estimator still
  produces no lead, no email."

  This recon RULES OUT hierarchy-contract divergence as the cause:
    • Helper called   — proven (R1)
    • Chain stamped   — proven (R2)
    • Fan-out wired   — proven (R3)
    • Header fix is sufficient at this layer — proven (R4)

  By elimination, the remaining root-cause candidates (from the
  prior recon, all still open):
    1. Operator hasn't actually re-submitted the estimator form
       since the deploy (post-deploy 30-min DB window shows no new
       estimator lead, but also no evidence of a submit attempt
       hitting the route).
    2. Vercel cache served pre-e79c670 build at the moment of the
       test.
    3. Client-side `if (!agentId)` silent-skip — fires BEFORE
       submitLeadFromForm. Devtools console capture would show
       `🔍 DEBUG EstimatorResults: { agentId: '...' }`.
    4. The submitLeadFromForm.ts:51 null-check still firing because
       the deployed middleware copy did not actually rebuild + ship
       (a deploy-ordering issue, NOT a code defect).

  None of these is a contract-conformance issue. The hierarchy
  fan-out + chain stamping + helper usage are all correct in code
  and would execute correctly once the path is reached.

═══════════════════════════════════════════════════════════════════════════
DELIVERABLE
═══════════════════════════════════════════════════════════════════════════

  recon/estimator-hierarchy-conformance.txt    ← this file

  No code edits. No commit. Estimator path CONFORMS to the locked
  Lead+Email contract at every checkpoint (helper called, chain
  stamped, fan-out wired). If the post-deploy estimator submit is
  still failing, the cause is NOT contract divergence — it's
  reachability of the createLead call site, which is the same surface
  the prior post-deploy recon flagged for live diagnostics
  (devtools console + Network tab capture, OR a fresh DB query with
  the actual test email).
