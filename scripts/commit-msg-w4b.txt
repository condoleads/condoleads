W4b: Plan tab renderer at email-template richness

New file: components/admin-homes/lead-workbench/PlanRenderer.tsx (~558 lines).
- Default export PlanTab({anchorLead, leadFamily}): discovers plan-bearing
  rows in family, renders selector pill row when >1 plans, defaults to
  anchor.
- Internal PlanRenderer({lead}): 10 sections gated on data presence -
  Market Intelligence (6-metric grid), Offer Intelligence (3-card), Best
  Time (seasonal), Subtype Breakdown (table), Summary (rare), Profile
  (buyer or seller variant), Top Listings, Source URL, Agent Card,
  Disclaimer.
- normalizePlan() handles BOTH plan_data shapes verified live via SQL:
    17 rows planType-nested shape (plan-email/route.ts writes)
    20 rows intent-flat shape (charlie/lead/route.ts writes)
- Empty plan_data -> "No plan generated" empty state. Empty sections
  do not render. No fabrication: renderer bounded by what is actually
  persisted to plan_data JSONB.

3 patches to existing files:
- LeadWorkbenchClient.tsx: import PlanTab; extend tab ternary with
  tab === 'plan' branch; PlaceholderTab still catches W4c-g.
- page.tsx: ANCHOR_SELECT + FAMILY_SELECT agent join extended from
  (id, full_name, email) to
  (id, full_name, email, cell_phone, profile_photo_url, brokerage_name, title)
  so AgentCard renders at email-template fidelity.
- docs/W-LEADS-WORKBENCH-TRACKER.md: v12 -> v13; W4b row OPEN -> SHIPPED;
  status log entry appended.

Findings logged:
- F-W4B-PLAN-DATA-RENDER-SUBSET: comparables, blocks, sellerEstimate,
  vipCreditUsed, summary, live agent object are API-time-only (not in
  plan_data); workbench cannot render them.
- F-W4B-PLAN-RENDERER-DRIFT-RISK: PlanRenderer.tsx (React JSX) and
  buildRichPlanEmail (HTML template string) are independent sources;
  future workstream should consolidate to shared lib/plan-renderer/.
- F-W4B-LISTING-LINKS-PENDING: top-listing rows are not click-through;
  requires tenant_domain resolution.
- F-W4B-ANALYTICS-EMPTY-IN-PRACTICE: real plan_data samples have
  analytics:{} and topListings:[]. Investigation belongs to
  plan-generation workstream.

Workflow note: chat delivery sanitization strips bare opening anchor
tokens not closed on the same line. Fix in
scripts/patch-w4b-fix-stripped-anchors.js uses runtime string
concatenation to avoid recurrence. Will use the same pattern for any
future patches that ship anchor-bearing JSX through chat.

Cleanup: scripts/commit-msg-w4a.txt orphan deletion (same pattern as
W4a cleaning up commit-msg-w3b.txt).

TSC --noEmit exit 0.

NEXT: W4c Credits & Usage tab (extract UserCreditPanel from Users page
surface; embed in workbench Credits tab).
