W4a + W3d: workbench page shell + click-row navigation

2 new files in app/admin-homes/leads/[id]/:
- page.tsx (server component): anchor lead fetch with hierarchy joins,
  cross-tenant gate via getScopedTenantId, permission gate via can('lead.read'),
  leadFamily aggregation by user_id within same tenant_id (cumulative view).
  Uses scope.ts helpers (W2.5's first consumer). 404 not 403 on cross-tenant
  + permission-deny + missing-lead (defense-in-depth, no existence leak).
- LeadWorkbenchClient.tsx (client): 7-tab nav (Overview rendered,
  Plan/Credits/Activity/Emails/VIP/Notes placeholder for W4b-g). Overview
  renders Lead Info dl + Hierarchy dl + Source URL link + All Events list
  (when family > 1) with anchor highlighted.

3 patches to components/admin-homes/AdminHomesLeadsClient.tsx (W3d):
- useRouter import from next/navigation
- const router = useRouter() hook
- <tr> onClick restored with router.push('/admin-homes/leads/' + lead.id);
  t.closest('button, input, select, a, label') short-circuit preserved.
  cursor-pointer class restored.

4 patches to docs/W-LEADS-WORKBENCH-TRACKER.md:
- v11 -> v12
- W4a phase row OPEN -> SHIPPED
- W3d phase row OPEN -> SHIPPED
- W4a+W3d-SHIPPED status log entry appended

Cleanup: scripts/commit-msg-w3b.txt orphan deletion (temp commit-msg file
from W3b git block was inadvertently committed before Remove-Item ran).

Multi-tenant safety: anchor's tenant_id is trusted source for all sibling
queries; cross-tenant access impossible by design.

Permission contract: can('lead.read') gates anchor; sibling leadFamily
shown without per-agent filter per outcome #3 (agent sees complete user
journey within tenant). F-W4A-LEADFAMILY-NO-PER-AGENT-SCOPE logged for
W5c evaluation.

TSC --noEmit exit 0.

NEXT: W4b Plan tab renderer (buyer + seller variants at email-template
richness).
