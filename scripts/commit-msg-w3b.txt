W3b: close as no-op (parity already exists)

W3b "Home property Book a Visit parity" closed with no code patch.

Recon over both property page clients confirmed Book a Visit CTA already
exists at parity:
- app/property/[id]/HomePropertyPageClient.tsx (12485 bytes) L179-L197
- app/property/[id]/PropertyPageClient.tsx (14842 bytes) L186-L207

Both render an identical collapsible Book-a-Visit button on the WALLiam
branch (isWalliam && walliamTenantId) toggling into <AppointmentForm
type="buyer" listings={[listing]} userId={user?.id} agent={null} ...>
from @/app/charlie/components/AppointmentForm.

4 CTAs per property type confirmed in WALLiam branch:
- WalliamAgentCard, WalliamCTA, Book-a-Visit, (Home)PropertyEstimateCTA

W1 recon finding "Home property page is missing the Book a Visit CTA"
is stale. Tracker bookkeeping only:
- v10 -> v11
- W3b phase row OPEN -> SHIPPED (no-op)
- W3b-CLOSED status log entry appended

2 new findings logged (deferred, NOT in W3b scope):
- F-PROPERTY-CLIENTS-DEAD-WALLIAM-CONTACT-FORM-IMPORT (both files import
  WalliamContactForm at L27/L30 but never use in JSX)
- F-APPOINTMENTFORM-AGENT-NULL-AT-PROPERTY-VISIT (AppointmentForm invoked
  with agent={null}; verify resolve_agent_for_context downstream)

P1 from W1 (showOfferModal && agent guard breaking Make-an-Offer when no
agent assigned) remains OPEN.

NEXT: W4a workbench page shell at /admin-homes/leads/[id]/page.tsx.
