// scripts/write-pinsview-p5-1-full.js
// W-TERRITORY-MASTER P5.1: replace PinsView.tsx with the full P5+P5.1 version.
// No anchors. Single write. ASCII-only enforcement before write.
//
// Strategy:
//   1. Verify current file is clean P5 (has 'MLS number' + 'C5678901', has no 'P5.1').
//   2. Backup current with timestamp.
//   3. Compose new file in memory via array.join(CRLF).
//   4. Verify ASCII purity byte-by-byte.
//   5. Write once.
//   6. Sanity-check the written file.

const fs = require('fs')
const path = require('path')

const TARGET = path.join(
  process.cwd(),
  'components', 'admin-homes', 'cockpit', 'territory', 'PinsView.tsx'
)

const original = fs.readFileSync(TARGET, 'utf8')
const usesCRLF = original.includes('\r\n')
const NL = usesCRLF ? '\r\n' : '\n'

// Sanity gate
if (!original.includes('MLS number') || !original.includes('C5678901')) {
  console.error('ABORT: Current file does not appear to be clean P5.')
  console.error('  Has "MLS number":', original.includes('MLS number'))
  console.error('  Has "C5678901":', original.includes('C5678901'))
  process.exit(1)
}
if (original.includes('P5.1')) {
  console.log('Current file already contains P5.1 markers. No changes needed.')
  process.exit(0)
}

console.log('Pre-state OK: clean P5 confirmed.')
console.log(`Line endings: ${usesCRLF ? 'CRLF' : 'LF'}`)
console.log('')

// Build the new file as an array of lines, joined with NL.
// Use single-quoted strings everywhere with explicit escaping; never embed
// non-ASCII characters.
const lines = [
  `'use client'`,
  `// components/admin-homes/cockpit/territory/PinsView.tsx`,
  `// W-TERRITORY-MASTER P5: Single-listing pins UI.`,
  `// W-TERRITORY-MASTER P5.1: search combobox (MLS + address).`,
  `// Operator can search MLS or address, pick an agent, pin (with reason), unpin, reactivate.`,
  ``,
  `import { useEffect, useMemo, useState } from 'react'`,
  `import { Pin, PinOff, RotateCcw, AlertCircle, Loader2 } from 'lucide-react'`,
  ``,
  `interface PinRow {`,
  `  id: string`,
  `  agent_id: string`,
  `  agent_name: string | null`,
  `  listing_id: string`,
  `  listing_mls_number: string | null`,
  `  listing_address: string | null`,
  `  listing_property_type: string | null`,
  `  listing_list_price: number | null`,
  `  listing_status: string | null`,
  `  is_active: boolean`,
  `  pin_reason: string | null`,
  `  created_at: string`,
  `  assigned_by: string | null`,
  `  assigned_by_name: string | null`,
  `  deactivated_at: string | null`,
  `  deactivated_by: string | null`,
  `  deactivated_by_name: string | null`,
  `}`,
  ``,
  `interface AgentOption {`,
  `  id: string`,
  `  full_name: string`,
  `  is_active: boolean`,
  `  is_selling: boolean`,
  `}`,
  ``,
  `interface SearchResult {`,
  `  id: string`,
  `  listing_key: string`,
  `  unparsed_address: string | null`,
  `  property_type: string | null`,
  `  list_price: number | null`,
  `}`,
  ``,
  `interface Props {`,
  `  tenantId: string`,
  `  actingAgentId: string | null // the operator's own agent_id; null = not logged in as an agent`,
  `}`,
  ``,
  `export default function PinsView({ tenantId, actingAgentId }: Props) {`,
  `  const [pins, setPins] = useState<PinRow[]>([])`,
  `  const [loading, setLoading] = useState(false)`,
  `  const [error, setError] = useState<string | null>(null)`,
  `  const [showAll, setShowAll] = useState(false)`,
  `  const [agents, setAgents] = useState<AgentOption[]>([])`,
  ``,
  `  // Pin form state`,
  `  const [pinMlsInput, setPinMlsInput] = useState('')`,
  `  const [pinAgentId, setPinAgentId] = useState('')`,
  `  const [pinReason, setPinReason] = useState('')`,
  `  const [pinSubmitting, setPinSubmitting] = useState(false)`,
  `  const [pinFormError, setPinFormError] = useState<string | null>(null)`,
  `  const [pinFormOk, setPinFormOk] = useState<string | null>(null)`,
  ``,
  `  // P5.1: search combobox state`,
  `  const [searchResults, setSearchResults] = useState<SearchResult[]>([])`,
  `  const [searchOpen, setSearchOpen] = useState(false)`,
  `  const [searchLoading, setSearchLoading] = useState(false)`,
  `  const [resolvedListingId, setResolvedListingId] = useState<string | null>(null)`,
  ``,
  `  // Action state`,
  `  const [actionRowId, setActionRowId] = useState<string | null>(null)`,
  ``,
  `  async function loadPins() {`,
  `    setLoading(true)`,
  `    setError(null)`,
  `    try {`,
  `      const params = new URLSearchParams({ tenant_id: tenantId })`,
  `      if (showAll) params.set('is_active', 'all')`,
  `      else params.set('is_active', 'true')`,
  `      params.set('limit', '500')`,
  `      const res = await fetch(\`/api/admin-homes/territory/pins?\${params.toString()}\`)`,
  `      if (!res.ok) {`,
  `        const body = await res.json().catch(() => ({}))`,
  `        throw new Error(body.error || \`HTTP \${res.status}\`)`,
  `      }`,
  `      const body = await res.json()`,
  `      setPins(body.data || [])`,
  `    } catch (e: any) {`,
  `      setError(e.message || 'Failed to load pins')`,
  `    } finally {`,
  `      setLoading(false)`,
  `    }`,
  `  }`,
  ``,
  `  async function loadAgents() {`,
  `    try {`,
  `      const res = await fetch(`,
  `        \`/api/admin-homes/territory/pins/agents-for-pinning?tenant_id=\${encodeURIComponent(tenantId)}\``,
  `      )`,
  `      if (!res.ok) return`,
  `      const body = await res.json()`,
  `      const list = body.data || []`,
  `      setAgents(list)`,
  `    } catch {`,
  `      // non-fatal`,
  `    }`,
  `  }`,
  ``,
  `  useEffect(() => {`,
  `    loadPins()`,
  `    loadAgents()`,
  `    // eslint-disable-next-line react-hooks/exhaustive-deps`,
  `  }, [tenantId, showAll])`,
  ``,
  `  // P5.1: debounced search effect (300ms). Clears resolved-listing on input change.`,
  `  useEffect(() => {`,
  `    const q = pinMlsInput.trim()`,
  `    setResolvedListingId(null)`,
  `    if (q.length < 3) {`,
  `      setSearchResults([])`,
  `      setSearchLoading(false)`,
  `      return`,
  `    }`,
  `    setSearchLoading(true)`,
  `    const handle = setTimeout(async () => {`,
  `      try {`,
  `        const res = await fetch(\`/api/admin-homes/listings/search?q=\${encodeURIComponent(q)}&limit=10\`)`,
  `        if (!res.ok) {`,
  `          setSearchResults([])`,
  `          return`,
  `        }`,
  `        const body = await res.json()`,
  `        setSearchResults(body.data || [])`,
  `      } catch {`,
  `        setSearchResults([])`,
  `      } finally {`,
  `        setSearchLoading(false)`,
  `      }`,
  `    }, 300)`,
  `    return () => clearTimeout(handle)`,
  `  }, [pinMlsInput])`,
  ``,
  `  async function resolveMlsToListingId(mls: string): Promise<string | null> {`,
  `    // P5.1: prefer the listing the operator explicitly picked from the dropdown.`,
  `    if (resolvedListingId) return resolvedListingId`,
  `    const trimmed = mls.trim()`,
  `    if (!trimmed) return null`,
  `    const res = await fetch(`,
  `      \`/api/admin-homes/listings/lookup?mls=\${encodeURIComponent(trimmed)}&tenant_id=\${encodeURIComponent(tenantId)}\``,
  `    )`,
  `    if (!res.ok) return null`,
  `    const body = await res.json()`,
  `    return body.data?.id ?? null`,
  `  }`,
  ``,
  `  async function submitPin() {`,
  `    setPinFormError(null)`,
  `    setPinFormOk(null)`,
  `    if (!actingAgentId) {`,
  `      setPinFormError('You must be logged in as an agent to pin listings.')`,
  `      return`,
  `    }`,
  `    if (!pinMlsInput.trim()) {`,
  `      setPinFormError('Enter an MLS number or pick from search')`,
  `      return`,
  `    }`,
  `    if (!pinAgentId) {`,
  `      setPinFormError('Pick an agent')`,
  `      return`,
  `    }`,
  `    if (pinReason.length > 500) {`,
  `      setPinFormError('Reason exceeds 500 chars')`,
  `      return`,
  `    }`,
  `    setPinSubmitting(true)`,
  `    try {`,
  `      const listingId = await resolveMlsToListingId(pinMlsInput)`,
  `      if (!listingId) {`,
  `        setPinFormError(\`No listing found for \${pinMlsInput}\`)`,
  `        return`,
  `      }`,
  `      const res = await fetch('/api/admin-homes/territory/pins', {`,
  `        method: 'POST',`,
  `        headers: { 'Content-Type': 'application/json' },`,
  `        body: JSON.stringify({`,
  `          tenant_id: tenantId,`,
  `          agent_id: pinAgentId,`,
  `          listing_id: listingId,`,
  `          assigned_by: actingAgentId,`,
  `          pin_reason: pinReason || null`,
  `        })`,
  `      })`,
  `      const body = await res.json().catch(() => ({}))`,
  `      if (!res.ok) {`,
  `        if (body.code === 'ALREADY_PINNED') {`,
  `          setPinFormError('Listing already has an active pin. Unpin it first.')`,
  `        } else {`,
  `          setPinFormError(body.error || \`HTTP \${res.status}\`)`,
  `        }`,
  `        return`,
  `      }`,
  `      setPinFormOk(\`Pinned \${pinMlsInput}\`)`,
  `      setPinMlsInput('')`,
  `      setPinReason('')`,
  `      setResolvedListingId(null)`,
  `      setSearchResults([])`,
  `      await loadPins()`,
  `    } catch (e: any) {`,
  `      setPinFormError(e.message || 'Failed to pin')`,
  `    } finally {`,
  `      setPinSubmitting(false)`,
  `    }`,
  `  }`,
  ``,
  `  async function deactivatePin(pin: PinRow) {`,
  `    if (!actingAgentId) {`,
  `      alert('You must be logged in as an agent to unpin listings.')`,
  `      return`,
  `    }`,
  `    if (!confirm(\`Unpin \${pin.listing_mls_number || pin.listing_id}? This routes the listing back to the geo cascade.\`)) {`,
  `      return`,
  `    }`,
  `    setActionRowId(pin.id)`,
  `    try {`,
  `      const res = await fetch(\`/api/admin-homes/territory/pins/\${pin.id}/deactivate\`, {`,
  `        method: 'POST',`,
  `        headers: { 'Content-Type': 'application/json' },`,
  `        body: JSON.stringify({ deactivated_by: actingAgentId })`,
  `      })`,
  `      if (!res.ok) {`,
  `        const body = await res.json().catch(() => ({}))`,
  `        alert(body.error || \`HTTP \${res.status}\`)`,
  `        return`,
  `      }`,
  `      await loadPins()`,
  `    } finally {`,
  `      setActionRowId(null)`,
  `    }`,
  `  }`,
  ``,
  `  async function reactivatePin(pin: PinRow) {`,
  `    if (!actingAgentId) {`,
  `      alert('You must be logged in as an agent to reactivate pins.')`,
  `      return`,
  `    }`,
  `    if (!confirm(\`Reactivate pin for \${pin.listing_mls_number || pin.listing_id}? If another active pin exists for this listing, this will fail.\`)) {`,
  `      return`,
  `    }`,
  `    setActionRowId(pin.id)`,
  `    try {`,
  `      const res = await fetch('/api/admin-homes/territory/pins', {`,
  `        method: 'POST',`,
  `        headers: { 'Content-Type': 'application/json' },`,
  `        body: JSON.stringify({`,
  `          tenant_id: tenantId,`,
  `          agent_id: pin.agent_id,`,
  `          listing_id: pin.listing_id,`,
  `          assigned_by: actingAgentId,`,
  `          pin_reason: pin.pin_reason`,
  `            ? \`Reactivated (was: \${pin.pin_reason.slice(0, 200)})\``,
  `            : 'Reactivated'`,
  `        })`,
  `      })`,
  `      const body = await res.json().catch(() => ({}))`,
  `      if (!res.ok) {`,
  `        if (body.code === 'ALREADY_PINNED') {`,
  `          alert('Cannot reactivate: another active pin exists for this listing.')`,
  `        } else {`,
  `          alert(body.error || \`HTTP \${res.status}\`)`,
  `        }`,
  `        return`,
  `      }`,
  `      await loadPins()`,
  `    } finally {`,
  `      setActionRowId(null)`,
  `    }`,
  `  }`,
  ``,
  `  const activeAgents = useMemo(`,
  `    () => agents.filter(a => a.is_active && a.is_selling),`,
  `    [agents]`,
  `  )`,
  ``,
  `  return (`,
  `    <div className="space-y-4">`,
  `      {/* Create form */}`,
  `      <div className="rounded-lg border border-gray-200 bg-white p-4">`,
  `        <div className="flex items-center gap-2 mb-3">`,
  `          <Pin className="w-4 h-4 text-green-600" />`,
  `          <h3 className="text-sm font-semibold text-gray-900">Pin a listing</h3>`,
  `        </div>`,
  `        <div className="grid grid-cols-1 md:grid-cols-4 gap-2">`,
  `          <div className="md:col-span-1 relative">`,
  `            <label className="block text-xs text-gray-600 mb-1">MLS or address</label>`,
  `            <input`,
  `              type="text"`,
  `              value={pinMlsInput}`,
  `              onChange={e => { setPinMlsInput(e.target.value); setSearchOpen(true) }}`,
  `              onFocus={() => setSearchOpen(true)}`,
  `              onBlur={() => setTimeout(() => setSearchOpen(false), 150)}`,
  `              placeholder="MLS or street name"`,
  `              className="w-full px-2 py-1.5 text-sm border border-gray-300 rounded"`,
  `              disabled={pinSubmitting}`,
  `              autoComplete="off"`,
  `            />`,
  `            {resolvedListingId && (`,
  `              <div className="mt-1 text-[10px] text-green-700">Listing selected</div>`,
  `            )}`,
  `            {searchOpen && pinMlsInput.trim().length >= 3 && (`,
  `              <div className="absolute z-20 left-0 right-0 mt-1 bg-white border border-gray-200 rounded shadow-md max-h-72 overflow-y-auto">`,
  `                {searchLoading ? (`,
  `                  <div className="px-3 py-2 text-xs text-gray-500">Searching...</div>`,
  `                ) : searchResults.length === 0 ? (`,
  `                  <div className="px-3 py-2 text-xs text-gray-500">No matches</div>`,
  `                ) : (`,
  `                  searchResults.map(r => (`,
  `                    <button`,
  `                      key={r.id}`,
  `                      type="button"`,
  `                      onMouseDown={e => e.preventDefault()}`,
  `                      onClick={() => {`,
  `                        setPinMlsInput(r.listing_key)`,
  `                        setResolvedListingId(r.id)`,
  `                        setSearchOpen(false)`,
  `                      }}`,
  `                      className="block w-full text-left px-3 py-2 text-xs hover:bg-green-50 border-b border-gray-100 last:border-b-0"`,
  `                    >`,
  `                      <div className="font-mono text-gray-900">{r.listing_key}</div>`,
  `                      <div className="text-gray-600 truncate">{r.unparsed_address || '(no address)'}</div>`,
  `                      <div className="text-[10px] text-gray-400">{r.property_type || ''}{r.list_price ? ' - $' + r.list_price.toLocaleString() : ''}</div>`,
  `                    </button>`,
  `                  ))`,
  `                )}`,
  `              </div>`,
  `            )}`,
  `          </div>`,
  `          <div className="md:col-span-1">`,
  `            <label className="block text-xs text-gray-600 mb-1">Agent</label>`,
  `            <select`,
  `              value={pinAgentId}`,
  `              onChange={e => setPinAgentId(e.target.value)}`,
  `              className="w-full px-2 py-1.5 text-sm border border-gray-300 rounded"`,
  `              disabled={pinSubmitting}`,
  `            >`,
  `              <option value="">- pick agent -</option>`,
  `              {activeAgents.map(a => (`,
  `                <option key={a.id} value={a.id}>{a.full_name}</option>`,
  `              ))}`,
  `            </select>`,
  `          </div>`,
  `          <div className="md:col-span-1">`,
  `            <label className="block text-xs text-gray-600 mb-1">Reason (optional, max 500)</label>`,
  `            <input`,
  `              type="text"`,
  `              value={pinReason}`,
  `              onChange={e => setPinReason(e.target.value)}`,
  `              placeholder="e.g. client requested this agent"`,
  `              maxLength={500}`,
  `              className="w-full px-2 py-1.5 text-sm border border-gray-300 rounded"`,
  `              disabled={pinSubmitting}`,
  `            />`,
  `          </div>`,
  `          <div className="md:col-span-1 flex items-end">`,
  `            <button`,
  `              onClick={submitPin}`,
  `              disabled={pinSubmitting || !pinMlsInput.trim() || !pinAgentId || !actingAgentId}`,
  `              className="w-full px-3 py-1.5 text-xs font-medium rounded bg-green-600 text-white hover:bg-green-700 disabled:bg-gray-300 disabled:cursor-not-allowed flex items-center justify-center gap-1.5"`,
  `            >`,
  `              {pinSubmitting ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Pin className="w-3.5 h-3.5" />}`,
  `              {pinSubmitting ? 'Pinning...' : 'Pin'}`,
  `            </button>`,
  `          </div>`,
  `        </div>`,
  `        {pinFormError && (`,
  `          <div className="mt-2 text-xs text-red-700 flex items-start gap-1">`,
  `            <AlertCircle className="w-3.5 h-3.5 mt-0.5 flex-shrink-0" />`,
  `            <span>{pinFormError}</span>`,
  `          </div>`,
  `        )}`,
  `        {pinFormOk && (`,
  `          <div className="mt-2 text-xs text-green-700">{pinFormOk}</div>`,
  `        )}`,
  `      </div>`,
  ``,
  `      {/* Filter row */}`,
  `      <div className="flex items-center gap-2">`,
  `        <button`,
  `          onClick={() => setShowAll(!showAll)}`,
  `          className={`,
  `            'px-3 py-1.5 text-xs font-medium rounded border ' +`,
  `            (showAll ? 'bg-gray-800 text-white border-gray-800' : 'bg-white text-gray-700 border-gray-300 hover:bg-gray-50')`,
  `          }`,
  `        >`,
  `          {showAll ? 'Showing all (incl. inactive)' : 'Active only'}`,
  `        </button>`,
  `        <span className="text-xs text-gray-500">{pins.length} pin{pins.length === 1 ? '' : 's'}</span>`,
  `      </div>`,
  ``,
  `      {/* Table */}`,
  `      <div className="rounded-lg border border-gray-200 bg-white overflow-hidden">`,
  `        {loading ? (`,
  `          <div className="p-6 text-center text-gray-500 text-sm flex items-center justify-center gap-2">`,
  `            <Loader2 className="w-4 h-4 animate-spin" /> Loading pins...`,
  `          </div>`,
  `        ) : error ? (`,
  `          <div className="p-6 text-center text-red-700 text-sm flex items-center justify-center gap-2">`,
  `            <AlertCircle className="w-4 h-4" /> {error}`,
  `          </div>`,
  `        ) : pins.length === 0 ? (`,
  `          <div className="p-6 text-center text-gray-500 text-sm">No pins to show.</div>`,
  `        ) : (`,
  `          <div className="overflow-x-auto">`,
  `            <table className="w-full text-xs">`,
  `              <thead className="bg-gray-50 text-gray-600 uppercase">`,
  `                <tr>`,
  `                  <th className="px-3 py-2 text-left">Agent</th>`,
  `                  <th className="px-3 py-2 text-left">MLS</th>`,
  `                  <th className="px-3 py-2 text-left">Address</th>`,
  `                  <th className="px-3 py-2 text-left">Type</th>`,
  `                  <th className="px-3 py-2 text-left">Reason</th>`,
  `                  <th className="px-3 py-2 text-left">Created</th>`,
  `                  <th className="px-3 py-2 text-left">Status</th>`,
  `                  <th className="px-3 py-2 text-right">Action</th>`,
  `                </tr>`,
  `              </thead>`,
  `              <tbody>`,
  `                {pins.map(p => {`,
  `                  const truncated = p.pin_reason && p.pin_reason.length > 60`,
  `                    ? p.pin_reason.slice(0, 60) + '...'`,
  `                    : (p.pin_reason || '')`,
  `                  return (`,
  `                    <tr key={p.id} className="border-t border-gray-100 hover:bg-gray-50">`,
  `                      <td className="px-3 py-2 font-medium text-gray-900">{p.agent_name || p.agent_id.slice(0, 8)}</td>`,
  `                      <td className="px-3 py-2 font-mono text-gray-700">{p.listing_mls_number || '-'}</td>`,
  `                      <td className="px-3 py-2 text-gray-700">{p.listing_address || '-'}</td>`,
  `                      <td className="px-3 py-2 text-gray-600">{p.listing_property_type || '-'}</td>`,
  `                      <td className="px-3 py-2 text-gray-600" title={p.pin_reason || ''}>{truncated || '-'}</td>`,
  `                      <td className="px-3 py-2 text-gray-500">{new Date(p.created_at).toLocaleDateString()}</td>`,
  `                      <td className="px-3 py-2">`,
  `                        {p.is_active ? (`,
  `                          <span className="inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-green-100 text-green-800">Active</span>`,
  `                        ) : (`,
  `                          <span className="inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-gray-200 text-gray-700">Inactive</span>`,
  `                        )}`,
  `                      </td>`,
  `                      <td className="px-3 py-2 text-right">`,
  `                        {p.is_active ? (`,
  `                          <button`,
  `                            onClick={() => deactivatePin(p)}`,
  `                            disabled={actionRowId === p.id}`,
  `                            className="px-2 py-1 text-[11px] rounded bg-white border border-gray-300 text-gray-700 hover:bg-red-50 hover:border-red-300 hover:text-red-700 disabled:opacity-50 inline-flex items-center gap-1"`,
  `                          >`,
  `                            <PinOff className="w-3 h-3" /> Unpin`,
  `                          </button>`,
  `                        ) : (`,
  `                          <button`,
  `                            onClick={() => reactivatePin(p)}`,
  `                            disabled={actionRowId === p.id}`,
  `                            className="px-2 py-1 text-[11px] rounded bg-white border border-gray-300 text-gray-700 hover:bg-green-50 hover:border-green-300 hover:text-green-700 disabled:opacity-50 inline-flex items-center gap-1"`,
  `                          >`,
  `                            <RotateCcw className="w-3 h-3" /> Reactivate`,
  `                          </button>`,
  `                        )}`,
  `                      </td>`,
  `                    </tr>`,
  `                  )`,
  `                })}`,
  `              </tbody>`,
  `            </table>`,
  `          </div>`,
  `        )}`,
  `      </div>`,
  `    </div>`,
  `  )`,
  `}`,
  ``
]

const content = lines.join(NL)

// Verify ASCII purity
for (let i = 0; i < content.length; i++) {
  const code = content.charCodeAt(i)
  if (code > 127) {
    const before = content.substring(Math.max(0, i - 30), i)
    const after = content.substring(i + 1, Math.min(content.length, i + 30))
    throw new Error(`Non-ASCII char (code ${code}) at offset ${i}. Context: "${before}[${String.fromCharCode(code)}]${after}"`)
  }
}
console.log('ASCII purity verified.')

// Backup current
const ts = new Date().toISOString().replace(/[-:.TZ]/g, '').slice(0, 14)
const backup = `${TARGET}.backup_p5_1_full_${ts}`
fs.writeFileSync(backup, original, 'utf8')
console.log('Backup:', path.basename(backup))

// Write
fs.writeFileSync(TARGET, content, 'utf8')
console.log('Wrote:', TARGET)
console.log(`Line count: ${lines.length}`)

// Post-write sanity
const written = fs.readFileSync(TARGET, 'utf8')
const writtenLines = written.split(/\r?\n/).length
console.log('')
console.log('Post-write sanity:')
console.log('  Line count matches:', writtenLines === lines.length, `(${writtenLines} vs ${lines.length})`)
console.log('  Has P5.1 marker:', written.includes('// P5.1: search combobox state'))
console.log('  Has resolvedListingId:', written.includes('resolvedListingId'))
console.log('  Has /listings/search:', written.includes('/api/admin-homes/listings/search'))
console.log('  Has MLS or address label:', written.includes('MLS or address'))
console.log('  No em dash:', !written.includes(String.fromCharCode(8212)))
console.log('  No ellipsis char:', !written.includes(String.fromCharCode(8230)))
console.log('  No check mark:', !written.includes(String.fromCharCode(10003)))
console.log('')
console.log('Verify with: npx tsc --noEmit && node scripts/r-w-territory-master-p5-1-smoke.js')